# 信用卡優惠資料蒐集與分類規格書

版本：v7（取代 v6-2026-07-30）
日期：2026-08-04（Asia/Taipei）
用途：交給資料整理人員，說明如何搜尋、整理、分類台灣信用卡與優惠資料。整理完成的資料會由工程端匯入「信用卡優惠查詢網站」。
閱讀對象：不需要懂程式，只需要會用 Google 試算表（或 Excel）與瀏覽器。

## v7 變更摘要（相對 v6）

1. **`offers` 工作表新增第 20 欄「回饋層級名稱」（選填），支援多層回饋**（見 5.3、3.5）。過去 `offers` 工作表只能表達「一檔優惠一組回饋」，多層回饋（如中國信託 LINE Pay 卡的一般消費／國外實體／指定商家三層）只能事後由後台表單手動補建，試算表無法承載。v7 起，**同一個「優惠代號」可以連續或不連續出現在多列**：第一次出現該代號的那一列要填齊優惠層級共同欄位（標題／分類／摘要／說明／來源網址／查證日期／日期範圍），同一代號後續的列（層級延續列）**只需要填回饋層級專屬欄位**（回饋層級名稱／回饋類型／回饋數值／回饋上限／最低消費／條件與限制），共同欄位可留空。匯入時依同一代號在表格中出現的順序建立多筆 RewardTier（依序 sortOrder = 0, 1, 2…）。單層回饋（絕大多數優惠）維持原本填一列的寫法，完全不受影響。
2. 對應模板：`信用卡優惠資料整理模板-v3-2026-07-29.xlsx` 已在 `offers` 工作表附加第 20 欄「回饋層級名稱」，欄位結構為既有欄位之後**附加**、不影響前 19 欄的順序與既有資料。
3. `scripts/import-offer-data.mjs` 同步改版：`validate()` 改為依代號分組驗證（共同欄位只在第一列檢查、層級欄位逐列檢查、不再把同代號多列判定為「代號重複」錯誤）；`runUpsert()`／`runReset()` 改為依分組建立對應筆數的 RewardTier，取代舊版「每列固定 1 層」的寫法。已用純函式單元測試（分組結果、tier 欄位對應與 sortOrder）與真實 xlsx 的 dry-run 驗證通過；因涉及正式站資料庫寫入的即時測試被系統風險分類器攔下，改以程式碼審閱＋純邏輯測試＋dry-run 三重方式確認正確性，尚未有一次「寫入正式站→查驗→清除」的完整實測（見下方風險提醒）。
4. 其餘章節內容不變。

## v6 變更摘要（相對 v5，仍然有效）

1. **消費情境標籤新增 5 項：`電影`、`高鐵台鐵`、`eTag`、`停車`、`道路救援`**（見 4.3）。參考同業網站常見的情境分類盤點後新增，涵蓋核心情境標籤總數來到 14 項。`高鐵`與`台鐵`合併為單一標籤`高鐵台鐵`（比照「繳稅」把牌照稅／房屋稅／地價稅合併的做法，避免標籤過度細碎）。
2. 不涉及欄位結構變動，`cards`／`offers` 工作表結構不變，沿用 `信用卡優惠資料整理模板-v3-2026-07-29.xlsx`，不需要新版模板。
3. 其餘章節內容不變。

## v5 變更摘要（相對 v4，仍然有效）

1. **消費情境標籤新增第 9 項：`訂閱服務`**（見 4.3）。涵蓋數位／串流／雲端訂閱等定期扣款服務（例如 Netflix、Spotify、YouTube Premium、Disney+、iCloud／雲端空間、線上課程訂閱等）。與其餘 8 個核心情境標籤（繳稅／學費／水電瓦斯／保費／加油／外送／超市量販／旅遊訂房）同等地位，適用時務必加上，供「訂閱 Netflix／Spotify 刷哪張卡」這類查詢使用。
2. 不涉及欄位結構變動，`cards`／`offers` 工作表結構不變，沿用 `信用卡優惠資料整理模板-v3-2026-07-29.xlsx`，不需要新版模板。
3. 其餘章節內容不變。

## v4 變更摘要（相對 v3，仍然有效）

1. **`cards` 工作表新增 5 個選填欄位：卡面示意圖配色**（見 5.2、4.5）。網站在卡片沒有「卡面圖片網址」時，會用參數化樣板自動生成卡面示意圖（晶片＋漸層底色＋卡片名稱），底色與文字顏色現在可以取自該卡官網卡面的真實色彩印象，取代原本純演算法雜湊的預設色。5 欄皆選填，留空會自動回退為系統預設色，不影響既有資料與既有卡片。
2. 對應模板：`信用卡優惠資料整理模板-v3-2026-07-29.xlsx`（沿用 v2 模板結構，只在 `cards` 工作表插入這 5 個新欄位）。
3. 其餘章節內容不變。

## v3 變更摘要（相對 v2，仍然有效）

1. **匯入方式改為增量更新，不再整批清空重建**（見 2.1）：之後每次交付只需要包含「新增」或「有變動」的列，沒有變動的既有資料不用重複填寫。
2. **新增半年更新週期的延續活動 vs 新活動判斷規則**（見 3.4）與對應 FAQ（Q10），解決銀行每半年改版權益時「優惠代號」該沿用還是換新的問題。
3. 其餘章節內容不變，與 T21（優惠條件結構化規劃）討論中暫緩的「多層回饋寫法慣例」是不同主題，該項仍維持 v2 原樣、未拍板。

---

## 1. 目標與優先順序

我們要建立一份「台灣信用卡優惠資料庫」，資料分成四種：

1. **銀行**：發卡銀行基本資料。
2. **信用卡**：每張卡的基本資料（卡片本身，不含優惠）。
3. **優惠**：每一筆優惠活動或卡片權益（網站的主要內容）。
4. **優惠 ↔ 卡片對應**：每筆優惠適用哪些卡。

**整理優先順序（由高到低）：**

1. 現金回饋類優惠（目前產品最優先）
2. 該優惠所屬的卡片與銀行基本資料（有優惠才需要建卡）
3. 餐飲、網購、旅遊、交通、分期類優惠

原則：**先有優惠，才回頭補卡片與銀行**。不要為了湊數把沒有優惠可寫的卡也建進來。

---

## 2. 交付格式

用一份 Google 試算表（或 Excel 檔），內含四個工作表：

| 工作表名稱 | 內容 | 一列代表 |
|---|---|---|
| `banks` | 銀行 | 一家銀行 |
| `cards` | 信用卡 | 一張卡 |
| `offers` | 優惠 | 一筆優惠 |
| `offer_cards` | 優惠與卡片對應 | 一組「優惠 × 卡片」關係 |

規則：

- 欄位名稱照第 5 節的規格，**不要自行增刪欄位或改欄位名稱**；有想補充的資訊寫在「備註」欄。
- 填不出來的欄位**留空白**，不要填「無」「N/A」「不確定」。真的有疑問寫在備註欄。
- 日期一律用 `YYYY-MM-DD` 格式（例：`2026-09-30`）。
- 每個工作表第一列是欄位名稱，從第二列開始填資料。

### 2.1 增量更新（v3 新增）

工程端匯入方式已改為**比對代號覆蓋或新增，不會整批清空重建**：

- 銀行代號／卡片代號／優惠代號在資料庫裡**已經存在**：這次表格裡同一代號的那一列會**整列覆蓋**資料庫裡的舊內容。
- 代號**不存在**：視為新資料，直接新增。
- 這次表格**沒有列出**的代號：資料庫裡的既有資料完全不受影響，不會被清空或刪除。

因此**每次交付只需要放進「這次要新增」或「這次有異動」的列**，沒有變動的既有銀行／卡片／優惠不需要重複填寫。但如果同一列裡只改了一兩個欄位，仍要把該列其他必填欄位都填齊整列交出（因為匯入是整列覆蓋，不是只改單一欄位）。

半年一次的優惠改版，「這筆優惠算延續（沿用代號）還是算新的（換代號）」，請見 3.4 的判斷規則。

---

## 3. 如何搜尋（資料來源規則）

### 3.1 來源優先順序

| 順位 | 來源 | 用途 |
|---|---|---|
| 1 | 銀行官網的**卡片介紹頁** | 卡片基本資料、長期權益 |
| 2 | 銀行官網的**活動/優惠頁**、官方權益手冊 PDF | 期間限定優惠的正式條款 |
| 3 | 銀行官方新聞稿、官方粉專公告 | 輔助確認活動存在與期間 |
| 4 | 部落格、比較網站（如卡優、Money101、PTT） | **只能當線索**，不能當最終依據 |

**鐵則：每一筆優惠的「來源網址」必須是官方頁面（順位 1–3）。** 從部落格看到的優惠，必須回到銀行官網找到對應頁面才能收錄；官網找不到就不收錄，可寫在備註欄回報。

**查詢起點**：`docs/data-collection/OFFICIAL_SOURCES_官方來源與輔助網址參考.md` 集中記錄各銀行/卡片已知的官方主要來源、官方輔助頁（FAQ／條款）、發現線索（部落格）與已觀察到的更新規律，更新既有卡片資料前可先查這份文件當起點（仍須實際核對官網現況，不能只憑文件舊記錄）。整理新卡或發現新的輔助網址時，也請順手補進這份文件對應段落。

### 3.2 建議搜尋方式

- Google 搜尋模式：`銀行名 + 卡名 + 優惠`、`銀行名 + 卡名 + 權益`、`卡名 + 現金回饋 + 2026`
- 限定官網搜尋：在 Google 加上 `site:` 指定銀行網域，例如：
  - `site:cathaybk.com.tw CUBE 回饋`
  - `site:bank.sinopac.com DAWHO`
- 直接逛銀行官網的「信用卡」→「優惠活動」清單頁，逐筆看。
- 比較網站可以用來**發現**有哪些卡和活動，再逐一回官網查證。

### 3.3 查證要求

- 每筆優惠記下你**實際確認官方頁面的日期**（填入 `查證日期` 欄）。
- 優惠條款以官方頁面當下內容為準；部落格寫的數字與官網不同時，一律以官網為準。
- 活動已結束（結束日在今天之前）原則上不收錄；除非是長期權益改版，收錄新版即可。

### 3.4 半年更新週期：延續活動 vs 新活動判斷（v3 新增）

信用卡優惠常常每半年（或每季）改版一次。改版時官網內容可能只是「同一檔活動延長期間」，也可能是「換成一檔條款不同的新活動」。這個差異會直接影響你這次要不要沿用舊的「優惠代號」，請依下列規則判斷：

**情況一：條款延續、只是延長期間**
官網內容（回饋數字、上限、條件）跟你上次整理的**完全一樣**，銀行只是把活動期間往後延（例如原本到 6/30，現在改成到 12/31）：

- **沿用同一個優惠代號**，這次表格裡照舊代號填這一列，只更新「結束日期」（其他欄位若有微調文字也一併更新）。
- 這樣工程端匯入時會直接覆蓋更新同一筆資料，不會產生重複的優惠。

**情況二：條款有實質變動，算新一檔活動**
回饋數字、上限、條件其中任何一項跟上次不一樣，等於銀行換了一檔新活動：

- **換一個新的優惠代號**，沿用既有的命名慣例加上新的期間後綴（例如原本 `-2026h1` 改成 `-2026h2`，或 `-2026q3` 改成 `-2027q1`）。
- 舊代號那筆優惠**不用刪除、也不用回頭改**，等它的結束日期一到，前台會自動不再顯示，資料庫留著當歷史紀錄即可。

**判斷不確定時**：只要回饋數字、上限或主要條件任何一項有變，一律當「情況二」處理（換新代號），不要為了省事沿用舊代號硬改內容——避免弄丟「這個代號在某個時間點條款是什麼」的歷史正確性。真的不確定就在備註欄寫清楚，交由工程端或使用者確認。

### 3.5 多層回饋怎麼填（v7 新增）

有些優惠不是單一回饋率，而是分好幾層（例如：一般消費 1%、國外實體加碼到 2.8%、指定商家再加碼到 16%）。這種情況：

- **同一個「優惠代號」填在連續的多列**，一層一列。
- **第一列**要填齊優惠層級共同欄位：優惠標題、優惠代號、主分類代碼、一句話摘要、詳細說明、開始／結束日期、來源網址、查證日期、標籤、備註。
- **第二列開始**（同一個優惠代號）：優惠代號欄要**重複填一樣的代號**，但共同欄位可以留空，只需要填這一層專屬的欄位：回饋層級名稱（例如「一般消費」「國外實體加碼」）、回饋類型、回饋數值、回饋上限、最低消費、條件與限制。
- `offer_cards` 工作表只需要對這個優惠代號填一次適用卡片，不用每層都重複填。
- 層級的先後順序＝表格裡列的先後順序，前台會依序顯示。
- 如果優惠本來就只有一層（絕大多數情況），維持原本「一列一優惠」的填法即可，不用特別處理「回饋層級名稱」欄，留空就好。

範例（3 層回饋）：

| 優惠標題 | 優惠代號 | 主分類代碼 | …（其餘共同欄位） | 回饋類型 | 回饋數值 | 條件與限制 | 回饋層級名稱 |
|---|---|---|---|---|---|---|---|
| LINE Pay卡回饋計畫 | linepay-rewards-2026h2 | cashback | （填齊） | 點數回饋 | 1% | 一般消費適用 | 一般消費 |
| （留空） | linepay-rewards-2026h2 | （留空） | （留空） | 點數回饋 | 最高2.8% | 限國外實體 | 國外實體加碼 |
| （留空） | linepay-rewards-2026h2 | （留空） | （留空） | 點數回饋 | 最高16% | 限指定商家 | 指定商家加碼 |

---

## 4. 如何分類

### 4.1 六大分類定義

每筆優惠必須歸入**恰好一個**主分類（填分類代碼）：

| 分類代碼 | 名稱 | 收什麼 | 例子 |
|---|---|---|---|
| `cashback` | 現金回饋 | 以「回饋 %數 / 現金 / 點數折抵」為主要賣點、不限定單一消費場景的優惠 | 一般消費 3% 回饋、新戶前三個月 8% |
| `dining` | 餐飲美食 | 限定餐廳、外送、餐飲通路的優惠 | 週四餐廳 5%、指定套餐折扣 |
| `travel` | 旅遊交通 | 旅遊、機票、訂房、機場服務、海外消費 | 海外消費加碼、機場接送、貴賓室 |
| `online-shopping` | 網購電商 | 限定電商平台或線上消費 | 蝦皮購物節加碼、指定網購 5% |
| `transport` | 交通通勤 | 日常通勤：加油、停車、大眾運輸、悠遊卡自動加值 | 悠遊卡自動加值回饋、加油降價 |
| `installment` | 分期零利率 | 以分期付款、零利率為主要賣點 | 3C 分期 0 利率、旅遊分期 |

### 4.2 分類判斷規則（衝突時依序套用）

一筆優惠常常同時沾到多個分類，依下列順序判斷主分類：

1. **看限定場景，不看回饋形式**：優惠若限定特定消費場景（餐廳、電商、海外…），歸入該場景分類。例：「指定餐廳現金回饋 10%」→ `dining`，不是 `cashback`。
2. **不限場景的回饋** → `cashback`。例：「國內消費不限通路 2%」。
3. **分期優先於場景**：主要賣點是「分期 / 零利率」→ `installment`，即使限定 3C 或旅遊。
4. **海外消費、機場、訂房** → `travel`；**日常通勤（加油、停車、捷運、悠遊卡）** → `transport`。兩者都像時：出遊性質 → `travel`，日常性質 → `transport`。
5. 還是無法判斷：先選你認為最接近的，並在備註欄寫「分類待確認：理由」。

### 4.3 標籤（tags）補充次要分類

主分類只有一個，其他相關面向用 `標籤` 欄補充。規則：

- 用逗號分隔，例如：`現金回饋,新戶,行動支付`
- 建議常用標籤：`新戶`、`行動支付`、`海外`、`加碼`、`需登錄`、`長期權益`、`期間限定`、`指定通路`
- **消費情境標籤（v2 新增，v5 擴充為 9 項，v6 擴充為 14 項）**：優惠適用下列消費情境時，務必加上對應標籤。這些標籤之後會用來回答「繳稅／繳學費刷哪張卡」這類查詢，是網站的重點功能：
  - `繳稅`（綜所稅、牌照稅、房屋稅、地價稅等）
  - `學費`（大專院校、中小學學費）
  - `水電瓦斯`（水費、電費、瓦斯費、電信費等公共事業費）
  - `保費`（保險費刷卡）
  - `加油`
  - `外送`（foodpanda、Uber Eats 等）
  - `超市量販`（全聯、家樂福、好市多等）
  - `旅遊訂房`（訂房平台、飯店、機票）
  - `訂閱服務`（v5 新增：數位／串流／雲端訂閱等定期扣款服務，例如 Netflix、Spotify、YouTube Premium、Disney+、iCloud／雲端空間、線上課程訂閱等）
  - `電影`（v6 新增：電影票、影城消費）
  - `高鐵台鐵`（v6 新增：高鐵、台鐵訂票與消費，兩者合併為單一標籤）
  - `eTag`（v6 新增：國道電子收費預繳／儲值）
  - `停車`（v6 新增：市區停車、路邊停車）
  - `道路救援`（v6 新增：免費道路救援權益）
- 特別注意：很多回饋活動**排除**繳稅、學費、保費、公共事業費。如果官網條款明確寫「排除」，不要加該情境標籤，並在「條件與限制」寫清楚排除項目；如果官網明確寫「適用」或有專屬加碼，才加標籤。
- 若一筆優惠本身是「切換方案／多種消費場景可選」（例如切換玩數位／樂饗購／趣旅行），每種方案對應的情境標籤都要各自補上，不要因為主分類只能選一個就漏掉其他方案的情境標籤。
- 標籤用詞盡量重複使用上面清單，不要同義詞混用（例如不要一下「新卡友」一下「新戶」）。

### 4.4 「活動」與「長期權益」都算優惠

- **期間限定活動**（有明確起訖日）：正常填 `開始日期`、`結束日期`。
- **長期卡片權益**（卡片本身的固定回饋，無結束日）：日期留空，標籤加 `長期權益`。

### 4.5 卡面示意圖配色（v4 新增，選填）

網站目前沒有取得任何銀行授權使用真實卡面照片。卡片沒有「卡面圖片網址」時，前台會用同一套樣板自動生成示意圖（圓角卡片＋晶片圖示＋卡片名稱），底色與文字顏色可以由你在 `cards` 工作表填入，取自**該卡官網卡面的整體色彩印象**。

規則：

- 只抽「整體是什麼顏色」，例如「黑底燙金字」「白卡銀灰字」「米黃底深棕字」。**不要**去描摹或重製銀行 Logo、卡片專屬圖案（例如某張卡卡面上的吉祥物、山形圖騰、菱形切面圖案）——這些是該銀行的專屬設計，只有整體色彩印象可以參考使用。
- 顏色請填 **`#RRGGBB` 六碼色碼**（例：`#DED5C6`），不要填顏色的中文名稱（例如「米色」）。可以用瀏覽器內建的開發者工具、線上取色工具，或請工程端協助從官網卡面截圖抽色。
- 5 個欄位可以只填部分，不需要五個都填。完全不填也不會出錯，網站會自動套用預設色。
- 如果同一張卡官網有多種卡面顏色可選（例如依電子票證功能分白/黃/藍三色），選一種你認為最具代表性的填入即可，並在「備註」欄註明還有其他顏色選項存在，供之後決定要不要進一步處理。

---

## 5. 欄位規格

### 5.1 `banks` 工作表

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 銀行名稱 | ✅ | 官方全名或通用簡稱，全表統一 | 國泰世華銀行 |
| 銀行代號 | ✅ | 英文小寫短代號，全表唯一，供對應用 | cathay |
| 官網網址 | ✅ | 銀行信用卡首頁 | https://www.cathaybk.com.tw/... |
| Logo 圖片網址 |  | 官方 logo 圖檔連結，找得到再填 | |
| 簡介 |  | 一兩句話即可 | |
| 備註 |  | 任何疑問或補充 | |

### 5.2 `cards` 工作表（v2 新增 6 欄；v4 新增 5 欄卡面配色）

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 卡片名稱 | ✅ | 官方卡名 | CUBE 卡 |
| 卡片代號 | ✅ | 英文小寫，用 `-` 連接，全表唯一 | cube-card |
| 所屬銀行代號 | ✅ | 對應 `banks` 的銀行代號 | cathay |
| 卡片官方頁面網址 | ✅ | 官網卡片介紹頁 | |
| 卡面圖片網址 |  | 官網卡面圖連結 | |
| 一句話摘要 | ✅ | 這張卡最主要的賣點，30 字內 | 權益可自選的現金回饋卡 |
| 詳細說明 |  | 卡片定位與主要權益概述（年費與等級改填下方專欄，不用重複寫在這裡） | |
| 適合對象 |  | 一句話 | 常換消費場景的小資族 |
| 年費 |  | 官網寫法照抄的短句；免年費就寫「終身免年費」 | NT$20,000 |
| 免年費條件 |  | 首年是否免、次年起的減免條件 | 首年免年費，次年起年刷 36 萬免年費 |
| 卡片等級 |  | 官網標示的卡別 | 無限卡 |
| 發卡組織 |  | Visa / Mastercard / JCB / 美國運通 | Visa |
| 卡面底色（起始） |  | 卡面示意圖底色漸層起始色，`#RRGGBB`，見 4.5 | #DED5C6 |
| 卡面底色（結束） |  | 卡面示意圖底色漸層結束色，`#RRGGBB` | #CFC5B3 |
| 卡片名稱文字顏色 |  | 卡面示意圖上卡片名稱的文字顏色，`#RRGGBB` | #6B7280 |
| 晶片顏色（起始） |  | 卡面示意圖晶片漸層起始色，`#RRGGBB` | #E5E7EB |
| 晶片顏色（結束） |  | 卡面示意圖晶片漸層結束色，`#RRGGBB` | #9CA3AF |
| 優點 |  | **一格內一行一項**（儲存格內換行），每項都要是官網可查證的事實，不寫主觀評價 | 國內 18 元/哩無上限（換行）每年 8 次機場貴賓室 |
| 注意事項 |  | **一格內一行一項**，寫申辦或使用時的限制與地雷 | 邀請制，無法主動申辦（換行）卡面有 Business 字樣，部分日本商家不受理退稅 |
| 備註 |  | | |

優點／注意事項填寫原則：

- 每項一行，在同一個儲存格內用換行分隔（Excel 按 `Alt+Enter`）。
- 只寫**官網或官方文件可查證的事實**（回饋率、附帶權益、限制條件），不寫「CP 值高」「很划算」這類主觀形容。
- 注意事項優先寫會影響申辦決定的事：邀請制、年費門檻、權益即將改版、排除項目等。

### 5.3 `offers` 工作表（主要工作量在這裡）

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 優惠標題 | ✅ | 讓使用者一眼看懂，格式建議「卡名/通路 + 重點」，25 字內 | CUBE 卡週四餐飲 5% 回饋 |
| 優惠代號 | ✅ | 英文小寫 + `-`，全表唯一；不會取就留空由工程端補；**若是既有優惠的半年改版，是否沿用舊代號見 3.4** | cube-dining-2026 |
| 主分類代碼 | ✅ | 六選一，見第 4 節 | dining |
| 一句話摘要 | ✅ | 40 字內講完「誰、做什麼、拿到什麼」 | 週四於國內餐廳單筆滿額，享 5% 刷卡金回饋 |
| 適合對象 |  | 一句話 | 每週固定外食的上班族 |
| 亮點一 |  | 最吸引人的一點，15 字內 | 週四餐飲 5% |
| 亮點二 |  | 次要亮點，15 字內 | 免登錄自動回饋 |
| 詳細說明 | ✅ | 完整描述優惠內容：怎麼參加、在哪消費、拿到什麼。可分段 | |
| 開始日期 |  | `YYYY-MM-DD`；長期權益留空 | 2026-01-01 |
| 結束日期 |  | `YYYY-MM-DD`；長期權益留空 | 2026-12-31 |
| 回饋類型 | ✅ | 只能填：`現金回饋`、`點數回饋`、`折扣`、`分期`、`其他` | 現金回饋 |
| 回饋數值 | ✅ | 主要數字，寫給人看的短句 | 5%（刷卡金） |
| 回饋上限 |  | 每期/每筆上限；多層上限就都寫出來 | 每月上限 100 元刷卡金 |
| 最低消費 |  | 門檻條件 | 單筆滿 1,000 元 |
| 條件與限制 | ✅ | 排除項目、需登錄、名額、不可合併使用等，逐條列出 | 需事先登錄；排除外送平台 |
| 來源網址 | ✅ | **官方頁面**網址（見 3.1） | |
| 查證日期 | ✅ | 你實際核對官網的日期 | 2026-07-08 |
| 標籤 |  | 逗號分隔，見 4.3（含 v2 新增的消費情境標籤） | 需登錄,期間限定,繳稅 |
| 備註 |  | 疑問、分類待確認、官網矛盾處都寫這裡 | |
| 回饋層級名稱（v7 新增） |  | 只有多層回饋才需要填，見 3.5；單層回饋留空即可 | 國外實體加碼 |

**多層回饋時的必填規則（v7 新增，見 3.5）**：上表標✅的欄位，只在「同一個優惠代號第一次出現的那一列」是必填；同一代號後續列（層級延續列）只有「回饋類型」「回饋數值」「條件與限制」三個欄位仍必填，其餘欄位（含標✅的優惠標題／主分類代碼／一句話摘要／詳細說明／來源網址／查證日期）可以留空。單層回饋（只有一列）不受影響，維持全部必填。

### 5.4 `offer_cards` 工作表

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 優惠代號 | ✅ | 對應 `offers` | cube-dining-2026 |
| 卡片代號 | ✅ | 對應 `cards` | cube-card |

- 一筆優惠適用 3 張卡，就填 3 列。
- 每筆優惠**至少要有一列**對應；出現在這裡的卡片代號必須存在於 `cards` 工作表。

---

## 6. 品質與驗收標準

交付前請自我檢查，工程端會照同樣標準抽驗：

1. **來源**：每筆優惠都有官方來源網址，且點開仍能看到該優惠。
2. **對應完整**：`offers` 每筆都在 `offer_cards` 有至少一列；`offer_cards`、`cards` 用到的代號都找得到對應列。
3. **代號唯一**：銀行代號、卡片代號、優惠代號在各自工作表內不重複。
4. **日期格式**：全部 `YYYY-MM-DD`；結束日期不早於開始日期；已過期活動不收錄。
5. **分類合法**：主分類代碼只出現六個代碼之一；回饋類型只出現五個選項之一。
6. **數字照抄官網**：回饋 %、上限、門檻不自行換算或推估；官網寫法模糊時照抄原文並在備註說明。
7. **留空原則**：查不到的欄位留空，不填猜測值。
8. **術語白話原則**：優惠內容出現銀行自家術語時（會員等級如「大戶Plus」、專案名稱、「指定任務」、自家點數名稱等），必須在「詳細說明」或「條件與限制」欄用白話解釋：該術語是什麼、一般人怎麼達成，並附上官方說明頁連結。檢驗標準：**一個從沒用過這家銀行的人，讀完後不需要另外查資料就能看懂**。寫不出解釋就代表你自己也還沒查清楚，回官網查到為止。
9. **優缺點可查證原則（v2 新增）**：`cards` 的優點與注意事項每一項都要能在官網或官方文件找到依據，不寫主觀評價；「注意事項」寫的是事實性限制（邀請制、排除項目、年費門檻），不是負評。
10. **情境標籤誠實原則（v2 新增）**：只有官網明確寫適用（或有專屬加碼）才加繳稅、學費等情境標籤；官網寫排除就不加，並在條件與限制寫明排除。

---

## 7. 常見狀況判斷（FAQ）

**Q1：同一張卡有 5 個優惠，要建幾列？**
`offers` 建 5 列（一列一優惠），`cards` 只建 1 列，`offer_cards` 建 5 列對應。

**Q2：一個活動好幾張卡都適用？**
`offers` 只建 1 列，`offer_cards` 每張適用卡各 1 列。不要為每張卡複製優惠。

**Q3：一個大活動裡面有好幾層回饋（例如一般戶 3%、新戶 8%）？**
若條件與期間相同，寫成 1 筆優惠，在「詳細說明」與「回饋數值」把層級寫清楚。若參加方式、期間或登錄要求不同，拆成多筆。

**Q4：優惠寫「最高 10%」但其實是多項加總？**
「回饋數值」照官網寫（例：最高 10%），「條件與限制」必須拆解各層條件，讓讀者知道 10% 怎麼組成。

**Q5：官網活動頁掛了或改版找不到？**
不收錄，寫在備註回報（附你原本看到的線索來源）。

**Q6：聯名卡的優惠來源是通路（如蝦皮）還是銀行？**
以發卡銀行官方頁為主要來源；通路官方公告可作為輔助來源一併附上。

**Q7：分不清楚是「活動」還是「長期權益」？**
官網有寫活動期間 → 活動（填日期）；寫在卡片權益頁、沒有期限 → 長期權益（日期留空 + 標籤 `長期權益`）。

**Q8：優惠條款裡出現銀行自己的名詞（例如「大戶Plus 加碼 4%」），要怎麼寫？**
不能照抄了事。實際案例：永豐 DAWHO 卡「大戶Plus 等級加碼 4%、大戶等級加碼 2.5%」——「大戶Plus」「大戶」是永豐 DAWHO 數位帳戶的會員分級，讀者不會知道。正確寫法是在詳細說明補上：等級怎麼分（大戶＝當月平均財富 30 萬元以上；大戶Plus＝100 萬元以上＋當月完成台股交易或換匯任務）、幾時生效（次月），並附官方分級制度連結。「指定任務」也一樣，必須寫出任務具體是哪幾件事（例如：綁定帳戶自動扣繳＋改用電子帳單，兩項同時達成）。見第 6 節第 8 條「術語白話原則」。

**Q9（v2 新增）：繳稅優惠通常是「活動」還是「長期權益」？**
多數是每年報稅季（5 月前後）的期間限定活動，照活動填起訖日期＋標籤 `繳稅,期間限定`。少數卡把繳稅回饋寫在常態權益，才算長期權益。牌照稅（4 月）、房屋稅（5 月）、地價稅（11 月）的活動期間不同，逐一照官網填。

**Q10（v3 新增）：上次交付的優惠這次官網改版了，要新增一列還是改原本那列？**
先判斷是「情況一：條款延續只是延長期間」還是「情況二：條款有實質變動」（見 3.4）。情況一就沿用原代號、只改結束日期；情況二就換新代號、當新的一列處理，原代號那列不用動、也不用刪除。**不確定就當情況二處理**（寧可多開一個新代號，也不要為了省事覆蓋掉舊條款的歷史紀錄）。

---

## 8. 建議工作流程

1. 選一家銀行，開官網信用卡優惠清單頁。
2. 先掃一輪，挑出**現金回饋類**優惠優先整理。
3. 每筆優惠：確認官方頁 → 填 `offers` 一列 → 確認適用卡片 → `cards`／`banks` 缺的補上 → 填 `offer_cards` 對應。
4. 一家銀行整理完，跑一次第 6 節的自我檢查。
5. 交付時註明：涵蓋哪些銀行、整理期間、已知未收錄項目（及原因）。

---

## 附錄：欄位與系統對照（工程端用，整理人員可略過）

| 工作表欄位 | 系統欄位（Prisma） |
|---|---|
| banks.銀行名稱/代號/官網/Logo/簡介 | Bank.name / slug / websiteUrl / logoUrl / description |
| cards.卡片名稱/代號/所屬銀行代號/卡面圖/摘要/說明/適合對象 | Card.name / slug / bankId(由銀行代號解析) / imageUrl / summary / description / targetAudience |
| cards.年費/免年費條件/卡片等級/發卡組織（v2） | Card.annualFee / annualFeeWaiver / cardLevel / cardNetwork |
| cards.卡面底色（起始/結束）/卡片名稱文字顏色/晶片顏色（起始/結束）（v4） | Card.cardBgColorFrom / cardBgColorTo / cardTextColor / cardChipColorFrom / cardChipColorTo（皆選填，格式不符 `#RRGGBB` 時忽略並回退預設色；試算表完全沒有這 5 欄時，不影響資料庫既有值） |
| cards.優點/注意事項（v2，儲存格內換行分項） | Card.prosJson / consJson（匯入時逐行轉 JSON 字串陣列） |
| offers.優惠標題/代號/主分類代碼 | Offer.title / slug / categoryId(由分類代碼解析) |
| offers.摘要/適合對象/亮點一/亮點二/詳細說明 | Offer.summary / targetAudience / highlight1 / highlight2 / description |
| offers.開始/結束日期 | Offer.startDate / endDate |
| offers.回饋類型/數值/上限/最低消費/條件與限制 | Offer.rewardType / rewardValue / rewardCap / minSpend / conditions |
| offers.來源網址/查證日期/標籤 | Offer.sourceUrl / lastVerifiedAt / tags |
| offers.回饋層級名稱（v7） | RewardTier.label（同一優惠代號的多列＝多筆 RewardTier，依列順序決定 RewardTier.sortOrder） |
| offer_cards | OfferCard(offerId, cardId) |

備註：回饋類型中文值與系統代碼對照由匯入時處理（現金回饋=cashback、點數回饋=points、折扣=discount、分期=installment、其他=other）。卡片官方頁面網址目前 schema 無對應欄位，先保留於試算表，未來 schema 討論時使用（見 real-card-data-field-check-summary-v1 開放問題）。
