# T30 日期時區修正 — 人工驗收腳本 v1

建立日期：2026-08-18（Asia/Taipei）
對象：使用者本人
正式站：`https://credit-card-web-pi.vercel.app`
狀態：**待執行**

> 自動驗證已通過：`node scripts/verify-t30-date-timezone.mjs`（19 項斷言，在 `TZ=UTC` 與
> `TZ=Asia/Taipei` 兩種環境下結果完全一致）、`tsc --noEmit`、`next lint`。
> 這份腳本只列**需要在正式站用人眼確認**的項目。

## ⚠️ 執行前提：必須先部署

**這個 bug 只在正式站出現**（本機時區剛好是台北，本機看起來一直都是對的）。
所以本腳本**必須在 T30 部署到正式站之後才能執行**，在本機跑沒有意義。

部署需要先取得使用者授權 push（2026-08-09 規則）。

## 對照表：修好之後應該看到什麼

以下是修正前正式站的實際錯誤值，取自 2026-08-18 的資料庫盤點（35 筆全部受影響）：

| 優惠 slug | 修正前（錯） | 修正後（對） |
|---|---|---|
| `cathay-cube-private-school-tuition-2026` | 2026/12/30 | **2026/12/31** |
| `ctbc-uniopen-core-rewards-2026h2` | 2026/8/30 | **2026/8/31** |
| `hsbc-cashback-signature-2026q3` | 2026/9/29 | **2026/9/30** |
| `taishin-richart-switch-rewards-2026h2` | 2027/3/30 | **2027/3/31** |
| `ctbc-linepay-rewards-2026h2` | 2026/12/28 | **2026/12/29** |

**通則：修正後的日期一律比修正前晚一天**，而且應該與官方活動頁上寫的結束日一致。

---

## A. 優惠詳情頁（動態頁，部署後即時反映）

| # | 檢查項目 | 結果 |
|---|---|---|
| A1 | 開 `/offers/cathay-cube-private-school-tuition-2026`，「活動期間」的結束日顯示 **2026/12/31**（不是 12/30） | |
| A2 | 同一頁的開始日顯示 **2026/7/1**（不是 6/30） | |
| A3 | 同一頁的「最後查證」顯示 **2026/8/18**（不是 8/17） | |
| A4 | 再開 `/offers/ctbc-uniopen-core-rewards-2026h2`，結束日為 **2026/8/31** | |

## B. 優惠卡片的「截至」（首頁、分類頁、情境頁、搜尋頁都用同一個元件）

| # | 檢查項目 | 結果 |
|---|---|---|
| B1 | 首頁「現在正在跑的活動」區塊，卡片上的「截至 …」與點進去的詳情頁**日期一致** | |
| B2 | `/search` 搜尋「學費」，結果卡片的「截至」也與詳情頁一致 | |
| B3 | 隨便挑一張卡片對照官方活動頁，日期正確 | |

> B1 特別重要：修正前「首頁卡片」與「詳情頁」其實是**同一個錯誤**（兩邊都少一天），
> 所以修正前看起來也是一致的。要對照的是**官方活動頁的真實日期**，不是兩頁互相比較。

## C. 過期判斷（最關鍵，但需要等到特定日期才看得出來）

| # | 檢查項目 | 結果 |
|---|---|---|
| C1 | **2026-08-31 當天**（台北時間下午，即 UTC 已進入 8/31 之後）確認這三筆仍出現在網站上：`ctbc-uniopen-core-rewards-2026h2`、`ctbc-uniopen-icashpay-stack-2026q3`、`ctbc-uniopen-overseas-physical-2026h2` | |
| C2 | 2026-09-01 當天，上述三筆已不再出現（正確過期） | |

> **C1 是這次修正的核心價值**：修正前，這三筆會在 2026-08-31 台北上午 8 點就消失，
> 但它們到當天結束都仍然有效。若 T30 未在 8/31 前上線，這個錯誤就會真實發生。

## D. 後台編輯往返（驗證日期不再往前跳）

> ⚠️ **注意**：後台直接連正式站資料庫，任何儲存都是真的寫入。
> D1、D2 只看不存，**不會寫入任何資料**；D3 會寫入，屬選做，執行前請先確認。

| # | 檢查項目 | 結果 |
|---|---|---|
| D1 | 登入後台 → 優惠管理 → 編輯 `cathay-cube-private-school-tuition-2026`，**不要儲存**。確認「結束日期」欄位顯示 **2026-12-31**（修正前會顯示 2026-12-30） | |
| D2 | 「開始日期」顯示 **2026-07-01**，「最後查證日期」顯示 **2026-08-18** | |
| D3 | （選做，會寫入資料）在 D1 的畫面直接按儲存，不改任何欄位。存完後重新進入編輯畫面，日期**完全沒有變**。修正前這一步會讓日期往前跳一天 | |

## E. 後台儀表板統計

| # | 檢查項目 | 結果 |
|---|---|---|
| E1 | 後台首頁的「過期優惠」數字合理（2026-08-18 時應為 0） | |
| E2 | 「即將到期」（未來 14 天）的數字與實際狀況相符 | |

## F. 攻略文章頁（目前 0 篇，發布第一篇後才需執行）

| # | 檢查項目 | 結果 |
|---|---|---|
| F1 | `/guides` 列表的「查證 YYYY-MM-DD」與後台填的日期一致 | |
| F2 | `/guides/[slug]` 的「最後更新」「最後查證」與後台一致 | |

---

## 回報方式

每項填 OK 或寫下看到的實際值。**C1 需要等到 2026-08-31 才能執行**，
其餘項目部署後即可進行；請先完成 A–E，C 留待該日再補。
