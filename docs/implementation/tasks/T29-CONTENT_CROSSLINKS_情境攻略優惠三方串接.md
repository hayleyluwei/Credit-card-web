# T29 情境／攻略／優惠三方串接

建立日期：2026-08-18（Asia/Taipei）
任務卡版本：v1.4（草案）
最後整理：2026-08-21（依當日唯讀盤點更新事實與待決問題）
核准狀態：待核准（(b)(c)(e) 已解決，(a)(d)(f)(g)(h) 待拍板）
問題類型：UX 改善／內容架構

> **狀態說明**：本任務卡只記錄規劃內容，**不構成任何實作授權**。使用者正式核准 Scope 之前，不得修改任何前台頁面、元件或資料。本卡唯一交付物即這份 Markdown。

## 背景

T28 把首頁情境卡改成問句式標題（例如「繳稅金額大，回饋能省下多少？」「學費也可以刷卡拿回饋嗎？」「保費一次繳，怎麼刷才有感？」），語氣變親近了，但**落地頁沒有跟上**。

使用者於 2026-08-18 指出這個落差：**卡片承諾了一個答案，點進去卻只有一份優惠清單。** 清單回答不了「能省多少」「划算嗎」這類問題——那是攻略文章的守備範圍。

這個問題在「帳單日常」那一組（繳稅／學費／水電瓦斯／保費）特別明顯：金額大、規則複雜、一年只做幾次，使用者要的是**結論**而不是**選項**。

同時，使用者也指出反方向的缺口：**攻略文章讀完之後應該能導回具體的優惠**——回到該情境的優惠列表，或直接導到特定卡片的優惠頁。目前攻略文章是內容的終點，不是行動的起點。

使用者於同日進一步指出第三個缺口：**優惠頁看不到自己的情境標籤**——查繳稅優惠時，明明有掛繳稅標籤的優惠，點進去卻只顯示「現金回饋」，沒有任何回到繳稅情境的入口。

因此本任務處理的是**情境、攻略、優惠三者互相到達**，不只是單向加連結。

### 目前已具備的基礎（不需重做）

- T20 已建立 `/guides`、`/guides/[slug]`、`Article` model 與後台 CRUD。
- 情境頁（`/scenarios/[slug]`）已有「slug 相同即視為對應攻略文」的關聯機制，找到文章時會顯示「延伸閱讀」區塊。
- T28 已建立 `SCENARIO_COPY` 對照表（`src/lib/domain-scenario-copy.ts`），情境卡的標題與行動文字集中管理。
- 攻略文章內文為 Markdown，已支援連結、表格與 callout 樣式。

### 真正的缺口

1. 首頁情境卡不論有無攻略文章，行動文字一律是「查看優惠」，指向情境頁。
2. 攻略文章沒有制度化的「導回優惠」出口，只能靠作者在 Markdown 裡自己寫連結。
3. 優惠頁不顯示情境標籤，也沒有連回情境頁的入口；標籤只存在於 JSON-LD，人看不到（詳見 Scope D）。
4. ~~**目前 0 篇已發布文章**，所以缺口 1、2 的連結改動在有文章之前都不會產生可見效果。~~
   **⚠️ 已過時（2026-08-21 實測）：現有 2 篇已發布文章**，見下方「現況重新盤點」。

---

## 現況重新盤點（2026-08-21，唯讀，未寫入任何資料）

本卡建立於 2026-08-18，其後專案有三項變動使卡片部分事實失效。以下為當日實際查證結果。

### 一、事實更正三條

| 卡片原本寫 | 實際狀況（2026-08-21 查證） |
|---|---|
| 「目前 0 篇已發布文章」 | **2 篇已發布**：`tuition`（學費）、`mid-autumn-dining-2026`（中秋聚餐），皆為 2026-08-18 上線 |
| 「本機 `.env` 直接指向正式站 Neon」 | **已於 2026-08-20 分離**，本機連 Neon `dev` 分支。安全限制需整段改寫 |
| 「migration 前必須備份正式站；`prisma/dev.db` 對 Postgres 無保護」 | 仍為真，但**已有 dev 分支可先行演練**，且 T33 已建立完整流程 SOP |

### 二、⚠️ 新證據：`slug` 相同的對應機制**已經破了**

實測 14 個情境 × 2 篇文章的對應結果：

| 文章 slug | 對應情境 | 結果 |
|---|---|---|
| `tuition` | `tuition` | ✅ 對得上 |
| `mid-autumn-dining-2026` | （無） | ⚠️ **對不到任何情境，是孤兒** |

**只有 2 篇文章就已經出現孤兒**，而且這不是意外——依架構文件 **D2／D3**，
中秋、母親節、報稅季這類**窄議題就是要用攻略文章承載、不新增 Category 也不做新頁面型態**。
所以**孤兒文章只會越來越多，這是設計的必然結果，不是資料沒填好**。

連帶後果：中秋那篇目前**只能從 `/guides` 進入**，任何情境頁都到不了它。

→ 這是待決問題 **(a)** 的決定性證據，見下方。

### 三、⚠️ 新發現：Scope D 的實作範圍取決於 T32 先做

D15 的動態規則是「標籤只要能連到實際存在的內容就帶連結」。實測目前的標籤現況：

**14 個情境標籤中，有 6 個目前 0 筆優惠**（動態規則下會是灰色不可點）：

| 有優惠（可點） | 0 筆（灰色） |
|---|---|
| 繳稅 2、學費 4、水電瓦斯 3、保費 3、加油 2、外送 3、超市量販 1、旅遊訂房 6 | 訂閱服務、電影、高鐵台鐵、eTag、停車、道路救援 |

**另有 51 種非情境標籤**（`期間限定`、`點數回饋`、`中信`、`LINE Pay`、`現金回饋`、`旅遊`…）。

問題在於：D15 的規則寫的是「能連到**優惠、分類、信用卡、攻略文章**」——
照字面解讀，`現金回饋` 連得到分類頁、`中信` 連得到銀行頁，**這 51 種有相當比例都會變成連結**。

**但 T32 Scope A 正要刪掉其中一批**（與分類同名的 `現金回饋`／`餐飲美食`／`網購電商`…、
以及多餘的上位詞 `旅遊`），並把同義詞合併。

→ **若 T29 先做，等於為一批即將被刪除的標籤實作連結邏輯。**
→ 這是本次整理發現的**最重要的排序結論**：**T32 應排在 T29 Scope D 之前**。

### 四、新發現：行動文字「一律相同」的前提本來就不成立

待決問題 (d) 假設「14 個情境是否一律相同文字」需要決定。實際查 `SCENARIO_COPY`：

**目前 15 筆設定中，14 筆是「查看優惠」、1 筆是「看權益」**（道路救援——因為它是卡片附帶權益，不是優惠活動）。

→ **依情境性質給不同文字，本來就已經是既有做法**，(d) 不需要重新確立原則，只需要決定新增的那一種文字怎麼寫。

### 五、Scope D 的主要測試案例仍然有效

`taishin-jkopay-card-bill-payments-2026`（台新街口聯名卡繳稅費最高 2.15%）仍存在，
標籤為 `繳稅,學費,水電瓦斯,行動支付,加碼,需登錄,期間限定`——
**同時具備「有優惠可連的情境標籤」與「非情境標籤」**，是驗證動態規則的理想案例。

## 已確認決策

- 本任務**不重新設計視覺**；一律沿用 T28 已定案的卡片生活誌風格契約。
- 不得產生死連結：沒有對應文章時必須自動退回現有行為。

### 2026-08-18 使用者拍板：文章導回優惠採「自動為主、手動優先」

**規則：預設自動依標籤撈取；若該篇文章有明確指定的導回目標，則以明確指定為準、覆蓋自動結果。**

這是刻意的雙軌設計，兩邊各自解決不同問題：

| | 自動（預設） | 手動指定（優先） |
|---|---|---|
| 來源 | 文章對應情境的標籤 | 作者於後台逐筆指定 |
| 優點 | 零維護；新增優惠自動納入 | 精準；可表達「我推薦這三筆」 |
| 適用 | 泛談某個情境的文章 | 有明確結論或排序的文章 |

**手動指定的目標可以是不同層級的連結**，由使用者的實例歸納：

| 使用者的例子 | 目標型態 |
|---|---|
| 文章談「CUBE 卡繳學費」→ 導到 `/offers/cathay-cube-private-school-tuition-2026` | **單一優惠** |
| 文章談「現金回饋」這個議題 → 導到 `/categories/cashback` | **分類** |
| 文章列「現金回饋前三名」→ 同時導到 `ctbc-linepay-rewards-2026h2`、`ctbc-uniopen-core-rewards-2026h2`、`taishin-jkopay-card-bill-payments-2026` | **多筆優惠，且順序有意義** |

因此手動指定必須支援：**多筆**、**混合型態**（優惠／分類，未來可能含卡片或情境）、**保留作者指定的順序**（「前三名」的名次不能被系統重排）。

上述五個網址已於 2026-08-18 實測皆回應 200，可作為實作與測試的真實案例。

## 目標

讓**情境、攻略文章、優惠**三者互相到達，形成走得完、回得來的路徑：

- 情境 → 攻略（Scope A）
- 攻略 → 優惠／分類／卡片（Scope B、C）
- **優惠 → 情境**（Scope D，2026-08-18 併入）

使問句式標題的承諾與落地頁實際提供的內容一致，並讓使用者從任一節點都能橫向探索，而不是每次退回首頁重新開始。

## Scope（規劃內容；核准實作前不得執行）

### A. 情境卡的行動文字隨內容自適應

| 情況 | 行動文字 | 連到 |
|---|---|---|
| 該情境**有**已發布攻略文章 | 例如「讀懂再決定」 | 攻略文章 |
| 該情境**沒有**文章 | 「查看優惠」（現況） | 情境頁 |

首頁需要知道每個情境有無對應文章，因此首頁查詢需增加一次文章 slug 的取得（僅取 slug 清單，不取全文）。

### B. 攻略文章導回優惠的制度化出口（雙軌：自動為主、手動優先）

> **⚠️ 優先度於 2026-08-20 上調為核心。** 原本本項被評為「有文章才看得到效果」的次要項目。
> 但 2026-08-20 的使用者動線收斂確認：**攻略文章是網站的外部入口**——使用者多半是看到
> 新聞或 KOL 貼文才被吸引過來，而且**落地的是文章本身，不是首頁**。
> 因此「讀完文章之後要去哪裡」是整個轉換路徑的關鍵，不是加分項。
> 依據：架構文件動線 #7 與 5.5。

依 2026-08-18 拍板：

1. **自動（預設）**：文章頁自動附上「相關優惠」區塊，依該文章對應情境的標籤撈取已發布優惠。作者不做任何設定就有基本效果，且新增優惠會自動納入。
2. **手動指定（覆蓋自動）**：`Article` 新增一個選填欄位，讓後台逐筆指定導回目標。有指定時完全取代自動結果，不混合顯示，避免作者精選的清單被自動撈的內容稀釋。

**手動指定需支援的能力**（見「已確認決策」的實例）：

- 多筆目標
- 混合型態：單一優惠（`/offers/...`）、分類（`/categories/...`），設計上保留卡片與情境的擴充空間
- **保留作者指定的順序**——「前三名」的名次由作者決定，系統不得重排

**這一項會改 schema**（新增 `Article` 選填欄位），須依 `AI_WORKFLOW` 走 schema 專門流程：先確認清單、備份、migration、同步 spec。

### C. 導回特定卡片的優惠

卡片詳情頁（`/cards/[slug]`）已有「這張卡的優惠」區塊，可直接作為落點。

實務上使用者的例子（「CUBE 卡繳學費」）其實**指向單一優惠而非整張卡**——因為那才是文章真正在談的東西。因此卡片層級的落點屬於 B 的手動指定所支援的型態之一，不需要另建機制；真正缺的是「某張卡 × 某個情境」這種交叉落點，但目前沒有對應 route，列為待決問題 (c)。

### D. 優惠頁顯示可點的情境標籤（2026-08-18 併入）

**起因**：使用者查繳稅優惠時發現 `taishin-jkopay-card-bill-payments-2026`（台新街口聯名卡繳稅費）「用繳稅查會找不到」。

**實測診斷結果——資料沒有問題，是呈現的問題**：

- 該優惠的標籤實際為 `繳稅,學費,水電瓦斯,行動支付,加碼,需登錄,期間限定`，**確實掛了繳稅**。
- `/scenarios/tax-payment` 有列出它、搜尋「繳稅」也找得到（2 筆），**兩條路徑都通**。
- 真正的缺口在**優惠頁本身**：麵包屑只呈現「首頁 / 現金回饋 / 標題」，標籤完全不顯示，連到情境頁的連結 0 個。標籤只存在於 JSON-LD 的 `keywords`——給搜尋引擎與 AI 讀的，**人看不到**。

**根本原因**：網站有兩套並行的分類軸——「分類」（6 種、每筆只能一個）與「情境標籤」（14 種、可多個）。這個雙軸設計本身正確（「什麼類型的回饋」與「能用在什麼場合」是兩件事），但目前只有分類軸在畫面上有存在感，情境軸在優惠頁是隱形的。

**做法**：優惠頁在摘要下方新增一列「這筆優惠可以用在」，列出該優惠的標籤。

**關鍵設計——兩種標籤必須外觀可辨**：

| 標籤 | 外觀 | 可點 | 連到 |
|---|---|---|---|
| **連過去有實際內容者** | 淡藍底、深藍字、帶箭頭 | 是 | `/scenarios/[slug]` 等對應頁 |
| **連過去沒有內容者** | 灰底灰字、無箭頭 | 否 | 無 |

**不得把所有標籤都做成連結**——沒有對應內容的標籤若做成連結會產生死連結，違反風格契約與本卡的既有原則。

> **⚠️ 判定規則已於 2026-08-20 改版（D15）**
>
> 原本的規則是**固定白名單**：「屬 14 個核心情境標籤者可點，其餘一律灰色」。
> 使用者於 2026-08-20 改為**動態規則**：
>
> > 標籤只要能連到實際存在的內容（相關優惠、分類、信用卡、攻略文章），就帶連結；
> > **沒有相關內容就不要帶連結。**
>
> **兩者目的相同（避免死連結），但新規則更好**：
>
> | | 固定白名單（原） | 有內容才可點（現） |
> |---|---|---|
> | 死連結風險 | 白名單內若剛好 0 筆優惠，仍會連到空頁 | 結構上不可能 |
> | 擴充成本 | 每次新增情境都要改白名單 | 零維護，隨資料自動生效 |
> | 與資料一致性 | 會漂移 | 永遠一致 |
>
> **連帶影響**：原本「本卡核准前必須先確定 14 個情境標籤最終清單」的前置條件**已解除**。
> 動態規則不需要事先決定清單，標籤可隨資料自然擴充。
>
> 依據：`docs/superpowers/plans/2026-08-20-INFORMATION_ARCHITECTURE_使用者動線與資訊架構收斂.md`（D15、5.6）

**效果**：使用者從任一優惠都能橫向跳到相關情境，不必每次退回首頁；原本只在結構化資料裡的標籤也變成真實站內連結。

**與 2026-07-29 決定的關係（必須記錄，避免被誤認為走回頭路）**：當時移除優惠詳情頁的「優惠摘要」側欄，理由是「純文字重複顯示分類與標籤，未接結構化資料、對 SEO 與 AI 引用零效果」。本次讓標籤重新出現**方向看似相反，但性質不同**——當時是純文字，現在是**可點的導覽入口**，具備實際導覽功能。這是有意識的轉向，不是推翻。


### E. 文件

更新任務索引與 CURRENT_STATE；完成後建立 Summary 與人工測試腳本。

## Non-scope

- **不撰寫攻略文章內容**（那是內容工作，不是本任務；但本任務的效果依賴至少一篇文章存在）。
- **不修改 T28 的風格契約**，不調整既有版面骨架與區塊順序。
- **不修改後台以外的權限行為**，不動登入流程。
- ~~不修改 Prisma schema~~ **(b) 已於 2026-08-18 拍板採雙軌，因此本任務確定會新增 `Article` 選填欄位**，須依 `AI_WORKFLOW` 走 schema 專門流程（確認清單 → 備份 → migration → 同步 spec）。除此之外不得變更任何既有欄位。
- 不修改既有優惠、卡片、銀行的資料內容。
- 不改變 SEO 結構化資料的既有輸出（新增連結不應改動 JSON-LD 型別）。
- 不新增套件。

## 安全限制

> **本節已於 2026-08-21 依 T33 的成果整段改寫。** 原本寫著「本機 `.env` 直接指向正式站 Neon」，
> 該事實自 2026-08-20 起已不成立。

- 因 (b)（與可能的 (a)）拍板需新增 `Article` 欄位，本任務**會動到 schema**，
  須依 `engineering-data-model-spec/schema修改流程.md` 先列清單取得確認，
  並依 `docs/sop/DATABASE_MIGRATION_資料庫結構變更流程.md` 執行。
- **本機連 Neon `dev` 分支**（2026-08-20 起）。schema 變更**先在 dev 演練並驗證**，
  確認無誤後才套用 production——這正是 dev 分支存在的意義。
- **套用 production migration 時：`npx prisma migrate deploy` 由使用者本人執行**
  （AI 執行會被 auto mode 擋下，T33 已實證），AI 負責前後的數字驗證。
- ⚠️ **部署順序**：Vercel 的 build 是純 `next build`，**不會自動套用 migration**。
  必須**先套 production 的 migration，再 push 依賴新欄位的程式碼**，
  否則正式站會在兩者之間的空窗期出錯。
- 除 migration 外不寫入任何業務資料；不得順手執行匯入或 seed。
- 若需 `next build` 驗證，先確認無共用 dev server 在跑。

## 影響範圍

- 頁面：首頁、`/scenarios/[slug]`、`/guides/[slug]`、**`/offers/[slug]`（Scope D）**、可能觸及 `/cards/[slug]`。
- 元件：`SCENARIO_COPY` 對照表、情境卡渲染、文章頁版面、**優惠詳情頁表頭（新增標籤列）**。
- 資料模型：**涉及**——`Article` 新增選填欄位以存放手動指定的導回目標。
- 外部服務：不涉及。

## 驗證方式與完成定義

- 自動驗證：`tsc --noEmit`、`eslint`、`next build`。
- **連結有效性**：每個新增連結都必須指向真實存在且可抵達的頁面；無對應文章時確認自動退回情境頁、不產生 404。
- 以真實資料實測：在有文章與無文章兩種狀態下各走一次，確認行動文字與落點都正確。
- 響應式：390／768／1440 三個斷點無破版。
- 人工驗收：使用者確認「情境 → 文章 → 優惠 → 情境」這個迴圈走得通且語意合理。
- **Scope D 專項**：確認核心情境標籤可點且連到正確情境頁；非核心標籤不可點、外觀有明顯區別；以 `taishin-jkopay-card-bill-payments-2026`（同時掛繳稅／學費／水電瓦斯）為主要測試案例。

## 資料保護與回復方式

> **2026-08-21 改寫**：原本寫「本機 `.env` 直接指向正式站 Neon」，該事實已不成立。

- **第一道防線是 dev 分支**：schema 變更先在 dev 完整演練與驗證，確認無誤才套 production。
- 新增的是**選填欄位**，既有 2 篇文章不受影響；若需回退，該欄位留空不使用即可。
- `prisma/dev.db` 的自動備份是 SQLite 時代遺留，**對 Neon Postgres 沒有保護作用**，不可依賴。
- 事後補救只有 Neon 的 **6 小時時間點還原**，且還原是整個分支回滾。
- 程式碼變更以 Git 版控保護；migration 檔進版控，兩個分支走同一個檔案。

## Git 授權

- 允許：status、diff、log 唯讀操作；本任務卡與治理文件更新。
- 不允許（核准時可另行授權）：建立或切換 branch、`git add`、local commit。
- **push 依 2026-08-09 規則一律需先詢問**（見 `CURRENT_STATE` 的「Git 授權規則」）。

## 風險與待決問題

> **2026-08-21 整理**：(b)(c)(e) 已解決，(a)(d)(f)(g)(h) 仍待拍板。
> 每題下方補上當日唯讀盤點的證據與建議。

1. **(a) 情境與文章的對應方式要不要沿用「slug 相同」？**

   原本的評估是「優點零設定；缺點是一個情境只能對應一篇文章，且文章 slug 被綁死」。

   **⚠️ 2026-08-21 實測後，這題的性質改變了——不是取捨，是已經壞掉。**

   只有 2 篇文章就已經出現孤兒：`mid-autumn-dining-2026` 對不到任何情境，
   **只能從 `/guides` 進入**。而依 D2／D3，窄議題文章（中秋、母親節、報稅季）
   本來就不會有對應情境，**孤兒是設計的必然結果**。

   **建議：改為後台明確指定對應情境**，並保留「slug 相同」作為未指定時的自動 fallback。

   | | slug 相同（現況） | 後台明確指定（建議） |
   |---|---|---|
   | 窄議題文章 | ❌ 永遠是孤兒 | ✅ 可掛到相關情境 |
   | 一個情境多篇文章 | ❌ 只能一篇 | ✅ 可多篇 |
   | 文章 slug 命名 | ❌ 被綁死，不利 SEO | ✅ 自由 |
   | 維護成本 | 零 | 每篇多選一次 |

   ⚠️ **這會擴大 schema 變更範圍**：原本只需新增 1 個「導回目標」欄位（Scope B），
   改為明確指定後還需要「對應情境」的欄位。兩者可在同一次 migration 完成。
2. ~~**(b) 文章導回優惠要用「自動依標籤撈取」還是「後台明確指定」？**~~ **已於 2026-08-18 拍板：兩者都要，自動為主、手動優先。** 詳見「已確認決策」與 Scope B。連帶確定**本任務會改 schema**（`Article` 新增選填欄位），須走 schema 專門流程。
3. **(c) 「某張卡 × 某個情境」的交叉落點要不要做？** 已釐清：單一優惠、分類、卡片這三種落點都由 (b) 的手動指定涵蓋，不需另建機制。真正缺的是「CUBE 卡在繳學費情境下的所有優惠」這種交叉頁面——目前沒有對應 route。**是否需要？** 若需要，等於新增 route（例如 `/cards/[slug]/scenarios/[scenario]`），會擴大 Scope 並影響 sitemap 與 SEO；若不需要，這類需求就用手動指定列出具體優惠來表達。
4. **(d) 情境卡的行動文字要怎麼寫？**

   **2026-08-21 查證：「是否一律相同文字」這個前提本來就不成立。**
   `SCENARIO_COPY` 現有 15 筆設定中，**14 筆「查看優惠」、1 筆「看權益」**
   （道路救援，因為它是卡片附帶權益不是活動）。**依情境性質給不同文字已是既有做法。**

   → 所以本題只剩「有文章時那一句怎麼寫」，不需要重新確立原則。
   **建議三選一由使用者拍板**：「讀懂再決定」／「先讀懂再刷」／「看懂再決定」。
   語氣與 T28 的問句式標題一致（回答問題），而不是「查看優惠」（提供清單）。

5. ~~**(e) 執行順序：先寫文章還是先改連結？**~~

   **已解決（2026-08-21）：前提消失。** 現有 2 篇已發布文章，
   其中 `tuition` 正好對應情境 `tuition`、且學費有 4 筆優惠，
   **是 Scope A 與 B 的現成測試案例**，不需要為了驗證而先寫文章。

6. **(f) 與 T27 的關係**：T27 的「本月情境選讀」也是導向情境或文章的入口，兩者的連結規則應一致，避免同一個目的地有兩套不同的導向邏輯。

   **2026-08-21 補充**：T27 卡片描述的現況**已被 T28 v2.1 取代**——
   卡片寫著「T28 負責版位預留」，但 T28 v2.1 已把該版位改成正式文案（推薦目標寫死為旅遊訂房）。
   兩張卡若要一致，**T27 需要先更新到現況**，否則對齊的是一個已經不存在的設計。
7. **(g) 手動指定的欄位格式怎麼設計？**（因 (b) 拍板而新增）需支援多筆、混合型態、保留順序。可能作法：存 JSON 陣列（如 `[{"type":"offer","slug":"..."},{"type":"category","slug":"..."}]`），或存路徑字串清單。前者結構清楚但後台輸入較繁瑣；後者輸入直覺但需驗證路徑是否存在、型態需由路徑推斷。**無論何者都必須在存檔時驗證目標真實存在**，否則會產生死連結——這正是 T28 的契約與本卡都明令禁止的。
8. **(h) 優惠頁的標籤列放哪裡、標題怎麼寫？**（Scope D）示意圖採「摘要下方、狀態徽章上方，以細線分隔，標題為『這筆優惠可以用在』」。位置與用字待確認。~~另需決定非核心標籤要顯示為灰色不可點，還是乾脆不顯示~~ → **後半已於 2026-08-20 由 D15 解決**：改為動態規則，有內容才可點、沒內容顯示為灰色純文字。

9. **(i) D15 的「有內容」要涵蓋到哪些頁型？**（2026-08-21 新增）

   D15 寫的是「能連到實際存在的內容（相關優惠、分類、信用卡、攻略文章）就帶連結」。
   **照字面解讀範圍相當大**——實測目前有 **51 種非情境標籤**，其中
   `現金回饋` 連得到分類頁、`中信` 連得到銀行頁、`LINE Pay` 可能連得到搜尋結果。

   若全部做成連結，優惠頁會出現一整排藍色標籤，反而稀釋「這筆優惠可以用在」的語意。

   **建議：Scope D 第一版只讓「情境標籤」可點**，其餘一律灰色。理由：

   - 標籤列的標題是「這筆優惠**可以用在**」——那是**場景**，不是發卡行或回饋形式
   - 分類與銀行在麵包屑、卡片區塊本來就已經有入口，不缺這一個
   - 範圍收斂後才能穩定驗收；未來要擴充是加法，不需回頭改設計

   這不違反 D15——D15 要防的是死連結，收斂範圍只會更少死連結，不會更多。

10. **(j) 與 T32 的先後順序**（2026-08-21 新增，**本次整理最重要的發現**）

    **T32 Scope A 正要刪掉一批標籤**（與分類同名的 `現金回饋`／`餐飲美食`／`網購電商` 等、
    多餘上位詞 `旅遊`）並合併同義詞；**T32 Scope C 則要補齊切換方案型優惠的情境標籤**
    （CUBE／Richart／玉山，目前全沒補）。

    → **若 T29 Scope D 先做**：等於為一批即將被刪除的標籤實作連結邏輯，
    而且 6 個目前 0 筆優惠的情境（訂閱服務、電影、高鐵台鐵、eTag、停車、道路救援）
    會全部顯示為灰色不可點——**但其中幾個正是 T32 Scope C 要補的**。

    **建議：T32 先做，T29 後做。** T32 不需要改 schema、不需要改程式，
    是純資料整理；做完之後 T29 面對的標籤集合才是穩定且正確的。

## 核准證據

- 核准者：（待核准）
- 核准日期與時區：（待核准）
- 核准 Scope 版本：（待核准）
- 核准原文或可追溯摘要：（待核准）
- Git 特別授權：（待核准）
- 高風險操作特別授權：（待核准）

## 核准前還缺什麼（2026-08-21 整理）

**技術面已經沒有障礙**——T33 打通了 schema 變更路徑，測試案例與資料都到位。
剩下的全是**產品判斷**，需要使用者拍板：

| # | 要決定的事 | AI 建議 | 影響 |
|---|---|---|---|
| **(a)** | 文章與情境的對應改為後台明確指定？ | **建議改**（slug 機制已出現孤兒） | 會多加一個 schema 欄位 |
| **(d)** | 有文章時的行動文字寫什麼？ | 「讀懂再決定」 | 純文案 |
| **(i)** | Scope D 第一版只讓情境標籤可點？ | **建議是**（收斂範圍） | 決定 Scope D 大小 |
| **(j)** | T32 先做還是 T29 先做？ | **建議 T32 先** | 決定開工順序 |
| **(f)** | T27 要不要先更新到現況？ | 建議先更新 | 影響兩卡一致性 |
| **(g)** | 手動指定欄位存 JSON 還是路徑清單？ | JSON 陣列 `[{type,slug}]` | 實作細節，可核准後再定 |
| **(h)** | 標籤列的位置與標題用字 | 沿用示意圖 | 實作細節，可核准後再定 |

**(g)(h) 屬實作細節，不阻擋核准**；(a)(d)(i)(j)(f) 建議在核准時一併拍板。

## Scope 變更紀錄

- v1.4／2026-08-21：依當日唯讀盤點整理，**未變更任何 Scope 內容，只更新事實與待決問題**。
  ① 更正三條過時事實（0 篇文章→2 篇；`.env` 已改連 dev 分支；migration 流程已由 T33 建立）。
  ② 新增「現況重新盤點」章節，記錄五項實測發現。
  ③ **(a) 的性質由取捨改為「已經壞掉」**——實測 2 篇文章即出現孤兒（`mid-autumn-dining-2026`
  對不到任何情境），且依 D2／D3 窄議題文章本來就不會有對應情境，孤兒是必然而非例外。
  ④ **(e) 已解決**（現有 2 篇文章，前提消失）；**(d) 收斂**（依情境給不同文字本來就是既有做法）。
  ⑤ 新增 **(i)** D15 的「有內容」要涵蓋哪些頁型（實測 51 種非情境標籤，建議第一版只做情境標籤）。
  ⑥ 新增 **(j)**：**T32 應排在 T29 之前**——T32 正要刪改一批標籤，T29 先做等於為即將消失的標籤寫邏輯。
  ⑦ 安全限制與資料保護兩節依 T33 成果整段改寫（dev 分支先行、production migration 由使用者執行、
  **先套 migration 再 push 程式碼**）。
- v1.3／2026-08-20：依 2026-08-20 資訊架構收斂文件更新兩處。① Scope D 的標籤判定規則由「固定 14 個白名單」改為 D15 的動態規則（有內容才可點），連帶解除「核准前須先確定標籤清單」的前置條件，待決問題 (h) 後半亦因此解決。② Scope B（文章導回優惠）優先度由次要上調為核心，因動線收斂確認攻略文章是網站的外部入口。
- v1.2／2026-08-18：新增 Scope D（優惠頁顯示可點的情境標籤），原「文件」順延為 Scope E，任務名稱由「雙向串接」改為「三方串接」。起因為使用者查繳稅優惠時發現找不到；實測確認資料無誤、情境頁與搜尋皆可找到，真正缺口在優惠頁本身不顯示標籤、也沒有連到情境頁的入口（標籤只存在於 JSON-LD）。明訂只有 14 個核心情境標籤可點，其餘標籤不得做成連結以免產生死連結；並記錄與 2026-07-29「移除標籤側欄」決定的性質差異（當時為純文字，本次為可點導覽入口）。新增待決問題 (h)。
- v1.1／2026-08-18：使用者拍板 (b)：文章導回優惠採「自動依標籤撈為主、手動明確指定優先」雙軌制；並以三個實例釐清手動指定需支援多筆、混合型態（優惠／分類）、保留作者順序。連帶確定本任務會新增 `Article` 選填欄位＝改 schema。(c) 收斂為「是否需要卡片×情境的交叉 route」；新增 (g) 手動指定的欄位格式與存檔驗證。
- v1／2026-08-18：建立草案。起因為使用者檢視 T28 上線後的首頁情境卡，指出「問句式標題承諾了答案，落地頁卻只有優惠清單」的落差，並進一步要求攻略文章需能導回相關優惠與特定卡片，形成雙向串接。記錄現有基礎（T20 的文章功能與 slug 對應機制、T28 的 `SCENARIO_COPY`）、三項真正缺口與 6 個待決問題，待核准。
