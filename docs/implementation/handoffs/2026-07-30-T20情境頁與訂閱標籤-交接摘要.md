# 2026-07-30 交接摘要（優惠摘要側欄清理、T23 收尾、T20 併入自動情境頁、情境標籤擴充至 14 項）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新）為準，本文件只做導覽。本文件涵蓋同一輪對話、但橫跨兩個日曆日的內容（2026-07-29 晚間至 2026-07-30 上午）：接續 2026-07-29 稍早的 `2026-07-29-T23-卡面配色與資料蒐集v4-交接摘要.md`，從確認該輪 3 個 commit 已 push 開始（2026-07-29 晚間），到優惠頁清理、T23 補齊最後 3 張卡配色（皆 2026-07-29 晚間），跨過午夜後（2026-07-30）繼續 CUBE 卡標籤缺口、T20 任務卡合併自動情境頁並擴充標籤為止。本文件所有內容日期已依 commit 實際時間戳核對修正，不沿用對話當下誤植的日期。T15/T16/T17/T18/T19/T21/T22/T24 現況本輪未觸碰，仍以前幾篇交接摘要為準。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`（工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`，Windows junction，同一份檔案）
- branch：`main`，HEAD：`3fd8e4d`
- 與 origin 關係：**完全同步，無待 push 的 commit**（本輪 3 個 commit `f0f6ecc`／`42c6108`／`3fd8e4d` 皆已 push）
- 新 session 務必重新執行 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 核對，不得只信本文件

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本輪對話全程不存在，未建立、未觸碰。
- 工作區目前只剩 2 個**與本輪無關、非本輪產生**的未追蹤檔案（不要動），以及 `docs/data-collection/backups/` 目錄（歷次備份，持續累積、不清除）：
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-china-airlines-20260728.xlsx`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-uniopen-20260728.xlsx`
- 本輪對資料庫（`prisma/dev.db`）做過 2 次直接寫入（皆非後台表單，皆先備份），詳見下方；資料庫本身不受 git 版控，不需要 commit。

## 本輪完成（依時間序）

**1. 核對並修正過時的 push 狀態記載**：交接摘要與 `CURRENT_STATE` 原記載「3 個 commit 尚未 push」，重新核對後發現其實已經 push 完成（HEAD 比記載的 `322049b` 多一個 commit `7910d70`，且與 `origin/main` 完全同步）。已更正文件內容。

**2. 優惠詳情頁清理（commit `f0f6ecc`）**：
- 使用者質疑優惠詳情頁的「優惠摘要」側欄（純文字顯示分類／標籤）對使用者與 SEO 是否有用。查證後確認：該區塊未接到任何結構化資料（`generateArticleJsonLd` 只有標題/描述/網址/日期），純粹是重複麵包屑的靜態文字。
- 使用者決定移除該側欄，並把標籤資料改寫進 `Article` JSON-LD 的 `keywords` 欄位（`src/lib/domain-seo.ts` 新增選填參數），讓標籤變成真正被 AI／Google 讀取結構化資料時能用到的內容。
- 檔案：`src/app/offers/[slug]/page.tsx`（移除側欄、版面改單欄）、`src/lib/domain-seo.ts`（`generateArticleJsonLd` 新增 `keywords` 參數）。已用瀏覽器讀出實際輸出的 JSON-LD 核對 `keywords` 值正確。

**3. T23 收尾：補齊最後 3 張卡的官網真實配色**（直接寫 `dev.db`，非 commit 內容）：
- momo卡、滙豐現金回饋御璽卡、星展eco永續卡此前一直是 fallback 色，本輪用瀏覽器 canvas 對官網卡面圖片做像素取樣（非肉眼截圖猜色）求出真實 hex 值：momo卡（白/淺灰卡身＋品牌桃紅文字，兩份官方素材交叉驗證）、滙豐現金回饋御璽卡（深炭灰，與同系列滙豐旅人御璽卡同屬黑卡系但深淺可辨識）、星展eco永續卡（森林綠，符合品牌調性）。
- 寫入前備份 `prisma/dev.db` 至 `prisma/backups/dev-before-t23-3new-cards-colors-20260729-220946.db`；寫入後用前台頁面讀出實際渲染的 SVG gradient stop 值核對與寫入值一致。
- **14 張卡至此全數有官網真實配色**，不再有卡片走 fallback 雜湊色。
- **多卡面選型缺口拍板**：不新增欄位記錄「選了哪一款」，改採「官網頁面讀取到的第一張卡片／色版」為代表色抽取原則，長期適用；本次星展eco系列（同時有 eco永續卡／永續極簡卡／優選／優選PLUS／世界商務／biz 等子卡）依此原則確認官網列表第一項就是本卡專屬頁面。玉山Unicard、中信LINE Pay卡現有配色不變。
- 產出並交付使用者一個 Artifact 頁面（`card-colors-review.html`）展示 3 張卡實際渲染的 SVG 卡面＋色碼＋來源說明。
- 提醒：這 3 張卡是 Codex 用舊版 v2 模板（無配色欄位）匯入的，原始 xlsx 完全沒有顏色欄，之後重跑同一份 xlsx 增量匯入不會覆蓋掉這次寫的顏色；但若之後升級這份 xlsx 到 v3 格式卻沒把顏色填進儲存格，依現行匯入邏輯「欄位存在但空白＝明確清空」會把顏色洗掉，需留意。

**4. commit＋push（`42c6108`）**：把上述 T23 收尾與 push 狀態更正寫進 `CURRENT_STATE_目前專案狀態.md` 並 push。

**5. CUBE 卡「玩數位」標籤缺口與情境標籤擴充第一輪（訂閱服務）**：
- 使用者提出「數位訂閱優惠該怎麼分類」，以國泰 CUBE 卡「切成玩數位可享 3% 回饋」為例。查證後發現：CUBE 這筆優惠（`cube-tiered-category-rewards-2026`）本身是「切換玩數位／樂饗購／趣旅行」三種方案的單一優惠，分類維持 `現金回饋` 正確（比照台新 Richart 卡同類模式），但標籤只涵蓋樂饗購／趣旅行對應的餐飲／旅遊訂房／外送／超市量販，漏了玩數位對應的情境。
- 使用者決定把「訂閱服務」升級為正式核心情境標籤（第 9 項，與繳稅／學費等同等地位），回補 CUBE 這筆優惠的標籤（DB＋來源試算表 `信用卡優惠資料整理模板-v2-2026-07-18.xlsx` 的 offers 工作表第 4 列，兩處改動前皆備份）。
- 規格書升版至 v5（`DATA_COLLECTION_SPEC...v5-2026-07-30.md`，v4 已加註取代說明），新增規則：切換方案類優惠每種方案的情境標籤都要各自標註，不要因主分類只能選一個就漏掉。
- 順手發現並補上：`/search` 搜尋頁的「熱門搜尋」快速入口原本沒有「訂閱服務」這個 chip，使用者不主動打字就找不到，已加入 `HOT_SEARCH_TERMS`（`src/app/search/page.tsx`）。
- 已驗證：前台 CUBE 優惠頁 JSON-LD `keywords` 正確帶出「訂閱服務」；搜尋頁點擊「訂閱服務」chip 正確篩出 CUBE 卡 1 筆結果。

**6. 情境搜尋 SEO 缺口討論 → T20 任務卡合併自動情境頁**：
- 使用者延伸討論「5 月繳稅季搜『繳稅 信用卡優惠』、9 月搜『學費 信用卡優惠』」的真實使用情境，質疑分類頁找不到對應內容。
- 查證確認：`/categories/[slug]` 有完整 `generateMetadata`＋JSON-LD，`/search?q=` 完全沒有（無論搜什麼，頁面標題都一樣），帶參數的搜尋結果頁對 Google／AI 是隱形的——情境標籤資料都在，但沒有可被索引的專屬網址。
- 使用者要求把這個問題正式排入 T20（攻略文章功能）任務卡討論。**過程中發現一個關鍵衝突**：T20 任務卡在 2026-07-27 已經明確決策「排除標籤自動聚合頁，只做人工攻略文章」，與這次討論的方向（自動情境頁）相反。已把這個衝突攤開給使用者。
- AI 用 `mcp__visualize` 做了一個自動情境頁的 mockup（`/scenarios/tax-payment` 樣式，用真實 2 筆繳稅優惠資料填充）供使用者評估內容單薄程度。使用者看過後**決定兩者都做**：自動情境頁負責「先有網址、全部標籤一次上線」，人工攻略文章負責「結論、比較表、FAQ」的深度內容，這個決定**取代**了 2026-07-27 的排除決定。
- T20 任務卡升版至 v2：Scope 新增 `/scenarios/[slug]`（比照 `/categories/[slug]` 做法，含 `generateMetadata`＋`WebPage` JSON-LD＋sitemap），Non-scope 拿掉排除自動聚合頁那條，新增「情境頁與攻略文章關鍵字互搶」風險條目、情境標籤對照表先寫死在程式碼（不開後台管理）等待決問題。

**7. 情境標籤第二輪擴充（14 項）**：
- 使用者貼了一張別的網站整理「日常生活消費/繳費/交通費」情境分類的截圖，指示「首刷禮行李箱不要，其他納入情境分類」。
- 比對後發現：截圖裡多數項目我們已有對應分類或標籤（高現金回饋／網購／外送／零利率分期／水電瓦斯／保費／學費／加油），「電話費」也已包在「水電瓦斯」標籤定義裡；真正新的只有 5 項：電影、高鐵、台鐵、eTag、市區免費停車、免費道路救援。
- 使用者確認高鐵／台鐵合併為單一標籤「高鐵台鐵」（比照「繳稅」合併牌照稅/房屋稅/地價稅的做法）。最終新增：`電影`、`高鐵台鐵`、`eTag`、`停車`、`道路救援`，核心情境標籤總數從 9 項來到 **14 項**。
- 規格書升版至 v6（`DATA_COLLECTION_SPEC...v6-2026-07-30.md`，v5 已加註取代說明）；T20 任務卡同步升版至 v3，Scope 內「涵蓋目前 N 個核心情境標籤」相關敘述皆同步改為 14 項。

**8. commit＋push（`3fd8e4d`）**：把 T20 任務卡 v2→v3、規格書 v5／v6、CUBE 標籤回補（DB＋xlsx）、`CURRENT_STATE`、任務索引、搜尋頁 chip 一併 commit＋push。

**9. 本文件建立前的最後一次文件核對**：重新通讀 `CURRENT_STATE_目前專案狀態.md` 全文，抓出並修正幾處未同步的殘留：HEAD 舊值、T20 版本號殘留寫 v2（實際已是 v3）、「下一步」清單裡「尚未 commit」的過時敘述、AI 引用性段落漏記優惠摘要側欄清理這件事、「既有未提交變更」段落誤把已 commit 的 T20 任務卡仍列為未追蹤草稿。以上皆已修正（尚未 commit，見下方待辦）。

## 目前資料庫狀態（2026-07-30 本輪結束時）

- 卡片總數：14 張，**全數已有官網真實配色**（本輪補齊最後 3 張）。
- 優惠總數：26 筆，CUBE 卡（`cube-tiered-category-rewards-2026`）標籤已補上「訂閱服務」。
- 核心情境標籤：14 項（原 9 項＋本輪新增電影／高鐵台鐵／eTag／停車／道路救援），其中新增的 5 項**目前尚無實際優惠資料掛標籤**，屬規格先行、資料待補，不影響現況運作。

## 尚未完成或待決

- **日期誤植修正紀錄**：本輪對話實際橫跨 2026-07-29 晚間至 2026-07-30 上午（`3fd8e4d` commit 時間戳為 07-30 08:44），但撰寫過程中沿用了對話開始時的 07-29 日期慣性，把跨午夜後決定的內容（規格書 v5／v6、T20 任務卡 v2／v3、CUBE 標籤回補、本文件）誤標成 07-29。使用者發現後已全部核對 commit 實際時間戳並修正日期與檔名（`v5-2026-07-29.md`→`v5-2026-07-30.md`、`v6` 同理、本交接摘要檔名同理）。新 session 若要新增文件，記得用 `date` 指令核對實際系統日期，不要沿用對話裡看到的舊日期字串。
- **T20 任務卡（v3）仍是草稿，尚未核准**：自動情境頁＋人工攻略文章＋14 個情境標籤的方向都已跟使用者確認過，但正式核准 Scope、進入實作階段尚未發生。核准前不得動 schema 或程式碼實作 `/scenarios/[slug]`、`/guides` 等頁面。
- **新增的 5 個情境標籤（電影／高鐵台鐵／eTag／停車／道路救援）目前資料庫裡沒有任何優惠掛這些標籤**：規格書已定義好，但還沒有實際資料驗證這些標籤在真實優惠上怎麼用；之後有相關優惠資料時要記得套用新標籤。
- **情境頁與攻略文章的關鍵字互搶風險未定案**：T20 任務卡已列為風險項，實作前需要再確認緩解方式（分工說法 vs canonical 導向）。
- T23 既有待辦不變：後台顏色欄位「儲存＋前台反映」完整迴圈待使用者找機會補測。
- T18／T21／T22／T24 現況維持不變，本輪未觸碰，詳見更早的交接摘要。

## 已確認且仍然有效的產品/流程決策（本輪新增）

- 優惠詳情頁不再顯示分類/標籤側欄，標籤改走 JSON-LD `keywords`。
- T23 多卡面選型原則：官網頁面讀取到的第一張卡片／色版為代表色抽取目標，長期適用，不新增資料庫欄位。
- 情境標籤「訂閱服務」＋「電影／高鐵台鐵／eTag／停車／道路救援」共 5 項正式併入核心情境標籤清單，總數 14 項；切換方案類優惠（如玩數位/樂饗購/趣旅行）每種方案的情境標籤都要各自標註。
- T20 方向反轉：自動情境頁與人工攻略文章**兩者都做**，非互斥，取代 2026-07-27 排除自動聚合頁的舊決定；情境標籤對照表第一版先寫死在程式碼，不開放後台管理。

## 驗證與環境

- 本輪驗證：`tsc --noEmit` 於 `src/app/offers/[slug]/page.tsx`／`src/lib/domain-seo.ts` 改動後、`src/app/search/page.tsx` 改動後皆執行過且通過；前台 JSON-LD、SVG 卡面渲染值、搜尋篩選結果皆用瀏覽器實測核對過與資料庫寫入值一致。
- 本機 dev server：本輪對話中偵測到**另一個 chat session 占用 port 3000**，本輪透過直接用瀏覽器開啟 `http://localhost:3000`（而非用 `preview_start` 啟動自己的 server 實例）借用該既有 server 完成所有前台驗證；新 session 若也遇到 port 3000 被占用，可比照做法，或視情況另開其他 port。
- Preview／Production 部署狀態：未知，本輪未涉及部署。

## 新 session 必讀文件

1. `AGENTS.md` → `AI_WORKFLOW_AI協作流程.md` → `CURRENT_STATE_目前專案狀態.md`
2. 本文件
3. `docs/implementation/handoffs/2026-07-29-T23-卡面配色與資料蒐集v4-交接摘要.md`（同一天稍早，T23 定案過程）
4. `docs/implementation/handoffs/2026-07-27-T16-T24-交接摘要.md`（T18/T21/T22/T24 現況仍以此為準）
5. `docs/implementation/tasks/T20-GUIDE_ARTICLES_攻略文章功能.md`（v3，待核准，本輪主要規劃產出）
6. `docs/data-collection/DATA_COLLECTION_SPEC_信用卡優惠資料蒐集與分類規格書-v6-2026-07-30.md`（資料蒐集現況，14 個核心情境標籤）

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD（應為 `3fd8e4d` 或更新）、Git status、`.ai-worktree-lock.json`，再讀 `docs/implementation/handoffs/2026-07-30-T20情境頁與訂閱標籤-交接摘要.md` 本文件與 `CURRENT_STATE_目前專案狀態.md`。本輪完成：優惠詳情頁清理（標籤改走 JSON-LD keywords）、T23 補齊最後 3 張卡官網配色（14 張全數完成）、CUBE 卡訂閱標籤回補、T20 任務卡升版 v3（合併自動情境頁＋人工攻略文章、情境標籤擴充到 14 項），規格書同步升到 v6；本輪對話跨越 2026-07-29 晚間至 2026-07-30 上午，過程中曾誤把跨午夜後的內容標成 7/29，已依 commit 實際時間戳全數修正。其餘待辦與待核准項目見上方「尚未完成或待決」。
