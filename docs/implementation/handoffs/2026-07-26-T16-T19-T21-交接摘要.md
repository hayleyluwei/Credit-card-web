# 2026-07-26 交接摘要（T16 資料匯入／T19 驗收／T21 決策）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新至 2026-07-26）為準，本文件只做導覽，不重複列出全部細節。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`
- branch：`main`，HEAD：本文件建立時為 `5bcec14`（新 session 請重新 `git rev-parse --short HEAD` 核對，本次交接後還會再多幾個 commit）
- 與 origin 關係：ahead 7＋，尚未 push

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本文件建立時由 **Codex** 持有，`status: ready_for_handoff`。新 session 寫入前務必先確認這把鎖是否還有效，不可自行刪除或接管。
- 已知長期未提交變更（T15 治理文件批次、首頁舊工作、T16 前置 `seed.mjs` 改寫、多張待核准任務卡草稿）：完整清單見 `CURRENT_STATE_目前專案狀態.md` 的「既有未提交變更」段落，**不得未經使用者同意擅自 commit**。

## 目前三條任務線（詳見 CURRENT_STATE）

1. **T16 第一版正式資料匯入**：已核准，資料已匯入（6 銀行／10 卡／16 優惠），匯入腳本已 commit，但 Summary、任務索引、CURRENT_STATE 的「正式標記完成」動作還沒做。
2. **T19 卡片結構化欄位**：已核准，功能已用真實資料驗證，但任務卡與索引仍顯示「待驗收」，需要正式改「完成」。
3. **T21 優惠條件結構化規劃**：草案待核准，(a)(b)(c)(d)＋規格書 v3＋T16/T21/T18 排序共六個分岔路**全部拍板完成**，只差使用者正式核准 Scope v1。

## 本輪完成（有 commit 或有實際驗證證據的部分）

- `d6abc55` T19 卡片欄位實作
- `64f7efa` T21 任務卡建立＋(a)(b)(c) 決策
- `cf0dc2d` T16 匯入腳本 `scripts/import-offer-data.mjs`
- `4746c10` T21 (d) 決策＋T16/T21/T18 排序子問題
- `f06be30`、`5bcec14`（使用者／Codex 自行 commit）：xlsx 試算表兩輪更新
- 兩輪真實資料匯入（第一輪 10 筆優惠，Codex 補繳稅／學費／水電瓦斯後累計 16 筆），前後台皆已用瀏覽器實際核對過畫面內容
- 針對 HSBC 兩筆優惠補了年費金額文字（僅資料庫層，重新匯入會被覆蓋，見 CURRENT_STATE）
- 對 T16 真實資料做過一次「AI 引用性」檢視，發現 `faqJson` 0/16 筆有填、多層回饋仍是散文——已寫入 CURRENT_STATE
- 本機管理員密碼重設過一次（密碼只告知使用者本人，未寫入檔案）

## 尚未完成或阻塞

- T16 Summary 未寫、任務索引與 CURRENT_STATE 的任務狀態欄位未正式改「完成」
- T19 任務狀態欄位未正式改「完成」（功能已驗證，只差文件動作）
- T21 Scope v1 未經正式核准，不得動 schema／程式碼
- `esun-unicard-wallet-new-card-2026q3` 的「最低消費」欄位內容疑似寫錯，待核對
- 2 筆優惠（永豐指定稅款分期、永豐學費回饋）因活動期已過（2026 上半年）目前不顯示在前台，非 bug
- 長期未提交變更批次仍未處理，非任何任務線的自然範圍

## 已確認且仍然有效的產品決策

- T21 六項決策：見上方「目前三條任務線」第 3 點
- 完整任務排序：T19 → T16 → T17 → T21 → T18 → T20
- 部署平台 Vercel Hobby + Neon Free（PostgreSQL）、後台帳號使用者本人持有（T18 既有拍板，尚未執行）
- 第一版資料庫完全清除 seed 測試資料（已完成）

## 驗證與環境

- 已執行：`node scripts/import-offer-data.mjs --dry-run`／正式匯入（兩輪，皆 0 錯誤）；本機瀏覽器實測多個前台頁面與部分卡片頁
- 未執行：lint／build 的分類仍是 `unclassified`；T17 定義的正式驗證腳本 `verify-release-data.mjs` 尚未建立
- 本機環境：dev server 用 `.claude/launch.json`（`npm run dev`, port 3000），本次對話結束時可能已停止，新 session 需要時自行重啟

## 下一步（唯一最優先）

**把 T16 收尾：寫 Summary、更新任務索引、把 T16／T19 任務狀態正式改成「完成」。** 其餘待辦（T21 核准、T17 開工、資料欄位小修正）見 `CURRENT_STATE_目前專案狀態.md` 下一步段落，不在本次交接中同時開工。

## 新 session 必讀文件

1. `AGENTS.md`
2. `AI_WORKFLOW_AI協作流程.md`
3. `CURRENT_STATE_目前專案狀態.md`（2026-07-26 版，本次交接的主要內容都在這裡）
4. `docs/implementation/tasks/T16-FIRST_RELEASE_DATA_IMPORT_第一版正式資料匯入.md`
5. `docs/implementation/tasks/T19-CARD_STRUCTURED_FIELDS_卡片結構化欄位與蒐集規格擴充.md`
6. `docs/implementation/tasks/T21-CONDITION_SCHEMA_優惠條件結構化規劃.md`
7. `docs/implementation/summaries/T19-CARD_STRUCTURED_FIELDS_SUMMARY_卡片結構化欄位與蒐集規格擴充摘要-v1-2026-07-18.md`

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD、Git status、`.ai-worktree-lock.json`（可能由 Codex 持有，寫入前要確認是否仍有效）與 `CURRENT_STATE_目前專案狀態.md`（2026-07-26 版），再讀 `docs/implementation/handoffs/2026-07-26-T16-T19-T21-交接摘要.md` 本文件與 T16／T19／T21 任務卡。先回報三條任務線各自的 Scope、Non-scope、核准版本、任務狀態與待決問題；不要在核對完成前寫入檔案。目前最優先的下一步是 T16 收尾（Summary＋任務索引＋CURRENT_STATE 正式標記完成）。
