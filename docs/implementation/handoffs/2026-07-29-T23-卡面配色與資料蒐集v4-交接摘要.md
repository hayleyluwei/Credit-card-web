# 2026-07-29 交接摘要（T23 卡面視覺定案＋schema v5＋資料蒐集規格 v4）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新）為準，本文件只做導覽。本文件涵蓋 2026-07-29 這整輪對話（從 T23 卡面視覺原型回饋開始，到 schema v5、資料蒐集規格 v4、Codex 交付批次匯入、環境清理為止），**不重複列出** T15/T16/T17/T19/T21 的完成細節與 T18/T20/T22/T24 的規劃現況——這些請見前一篇 `docs/implementation/handoffs/2026-07-27-T16-T24-交接摘要.md`，本輪對這幾條任務線沒有新增動作。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`（工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`，Windows junction，同一份檔案）
- branch：`main`，HEAD：`322049b`
- 與 origin 關係：`origin/main` 落後 3 個 commit（`f512fdb`→`e6ea3a5`→`322049b`），**本輪 3 個 commit 皆尚未 push**
- 新 session 務必重新執行 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 核對，不得只信本文件

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本文件建立時**不存在**（本輪結束前已釋放）。本輪對話中途曾發現 Codex 持有的鎖（`codex-202607-card-recommendation-xls-2026-07-29`，`status: ready_for_handoff`），使用者直接指示接手修改其交付的 xlsx，視為交接確認；接手時該鎖已不存在（Codex 已自行釋放）。新 session 開始時仍要重新檢查一次。
- 工作區目前只剩 2 個**與本輪無關、非本輪產生**的未追蹤檔案（不要動）：
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-china-airlines-20260728.xlsx`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-uniopen-20260728.xlsx`
- 本輪新增的 `docs/data-collection/backups/` 目錄（多個 xlsx 修改前備份）與 `prisma/backups/` 下新增的多個 `.db` 快照，皆保留、未清除。

## 本輪完成（T23：從視覺回饋到正式上線，依時間序）

**起點**：使用者對 2026-07-27 已建好的 T23 SVG 卡面原型（Artifact `01828680...`）給回饋，一路迭代到最終定案並寫入正式程式碼＋資料庫。

**視覺方向迭代**（皆先做 Artifact 原型驗證，確認後才動正式程式碼）：
1. 同銀行不同卡片撞色問題 → 底色雜湊改依「卡片代號」而非「銀行代號」，並發現原雜湊函式在小調色盤下仍會撞色，改用 FNV-1a＋12 色調色盤解決。
2. 使用者一度提議「油畫濾鏡套用官方卡面照片」，AI 分析後指出這仍是對官方素材的衍生重製、未真正降低著作權風險，使用者理解後放棄。
3. 視覺質感提升（漸層＋光澤＋金屬晶片＋金色細邊框 v2 版）。
4. 使用者回饋「太醜」後的多輪細修：移除發卡組織標籤（資料不全時畫面不一致）、卡片名稱改為卡面主視覺大字（含中英文混排斷行 `wrapCardName`）、金色細邊框從「僅頂級卡別顯示」改為「全部卡片一律套用、邊框跟著晶片色調走」。

**核准與正式實作**（使用者 2026-07-29 核准 T23 Scope，commit `f512fdb`）：
- 新增 `src/lib/cardVisual.ts`（FNV-1a 雜湊、12 色調色盤、`wrapCardName` 斷行）、改寫 `src/components/CardImage.tsx`（SVG 生成卡面）、4 個呼叫端頁面（首頁／`/cards`／優惠詳情頁／`/banks/[slug]`）多傳入必要欄位。

**真實配色方向**（使用者決定改抽官網真實色彩，取代純演算法雜湊）：
- 風險評估：只抽「整體色彩印象」（黑底燙金、白卡銀字等），不重製 Logo 或卡片專屬圖案（富士山、街口小豬、菱形切面、森林剪影一律不畫），風險遠低於直接用官方照片。
- 11 張卡全數用瀏覽器直接開官網截圖確認顏色（而非文字搜尋猜測），過程中抓出並修正 3 個猜錯的顏色：台新街口聯名卡（猜橘色→實際紅色）、台新Richart卡（猜藍色→實際金屬銀灰）、滙豐旅人無限卡（猜黑色→實際深藍紫菱形切面）。
- 顏色存放位置：使用者在「存資料庫」vs「存程式碼對照表」間選定**資料庫**，關鍵理由是未來銀行陸續授權真實圖片會是零星、長期混雜共存的狀態（`Card.imageUrl` 有值即顯示真圖），兩種呈現方式放同一張表才能維持單一後台維護流程，不會被拆成「一部分後台填、一部分找工程端」。

**schema v5**（依 `schema修改流程.md` 專門流程執行，先列清單經使用者確認才動檔案，commit `e6ea3a5`）：
- `Card` 新增 5 個可選欄位：`cardBgColorFrom`／`cardBgColorTo`／`cardTextColor`／`cardChipColorFrom`／`cardChipColorTo`。邊框顏色跟著晶片色調走，不另設欄位。
- 產出：`schema-checklist-2026-07-29.md`、`prisma-schema-spec-v5-2026-07-29.md`、`schema-v5-2026-07-29.prisma`。
- `prisma db push`（純新增可選欄位，無 data loss）、`prisma generate`、`tsc`、`eslint`、`next build` 皆通過。
- 後台 `AdminCardForm` 新增 5 個顏色輸入欄位（含填寫說明），`admin-actions.ts` 的 `cardData()` 納入新欄位。
- 驗證：fallback 路徑（欄位留空）確認畫面與 v5 前完全相同；填入 11 張卡真實色後前台正確反映，邊框跟著晶片色。**使用者已登入後台確認顏色欄位存在**；是否實際測試過「改顏色存檔→前台反映」這個完整迴圈，本文件無法確認，之後若要驗證可請使用者再測一次或由 AI 之後找機會補測。

**兩處前台收尾**（使用者實際瀏覽後回報的問題，同一輪處理完）：
- `/cards/[slug]` 卡片詳情頁原本自己寫了一份舊的「色塊＋首字」程式碼、完全沒有走 `CardImage` 元件，因此前面所有視覺迭代都沒套用到這一頁——已改為呼叫 `CardImage`，移除變成沒用到的 `next/image` import。
- `/banks/[slug]` 前台外露「銀行資訊」面板（銀行名稱／卡片數量／**SEO 標題**），SEO 標題屬內部欄位不該讓訪客看到（同類問題先前已處理過「是否啟用」「推薦分數」的外露，這次是漏網的一個）；移除後版面從雙欄改單欄，附帶解決了卡片名稱被擠成一字一行的排版問題。

**Codex 資料交付批次後續**（commit `322049b`）：
- Codex 於本輪對話中途交付 `信用卡優惠資料整理模板-v2-2026-07-18-202607推薦卡-20260729.xlsx`（8 家銀行、8 張卡、8 筆優惠），鎖狀態 `ready_for_handoff`。
- 發現並修正卡片代號打字錯誤：`ctbc-linepay-card` vs 資料庫既有的 `ctbc-line-pay-card`，若不修正會產生重複的中信 LINE Pay 卡（原始檔已備份）。
- 該批交付用的是舊版 v2 模板結構，沒有卡面配色欄位。順勢把資料蒐集模板升級為 **v3**（`信用卡優惠資料整理模板-v3-2026-07-29.xlsx`，新增 5 個選填卡面配色欄位，範例列直接用已確認的真實色）與**規格書 v4**（新增 4.5 節「卡面示意圖配色」規則）。v2 模板 xlsx 與 v3 規格書 `.md` 皆已加註「本版已被取代」指向新版。
- `scripts/import-offer-data.mjs` 同步支援讀取這 5 個欄位；過程中發現並修正一個**潛在資料遺失 bug**：舊版模板（欄位在物件裡是 `undefined`）增量匯入時，若不分青紅皂白一律寫入 `null`，會把資料庫裡既有卡片已填好的顏色整批洗掉。修正為「試算表表頭完全沒有這個欄位＝不動既有值，欄位存在但儲存格空白＝明確清空」，並用 dry-run＋真實匯入雙重驗證這 4 張既有卡（Richart／Unicard／DAWHO／CUBE）的顏色匯入後原封不動保留。
- 正式匯入結果：新增 2 家銀行（台北富邦、星展）、3 張新卡（momo卡、滙豐現金回饋御璽卡、星展eco永續卡）、5 張既有卡更新、6 筆新優惠、8 筆優惠卡片對應。匯入前後皆備份 `dev.db`。確認中信 LINE Pay 卡無重複、既有 4 張卡顏色無流失。

**環境清理**（使用者確認後執行）：
- 刪除 Codex 本輪任務的中間工作檔（皆為已完成任務的衍生副本，非唯一來源）：`outputs/2026-07-card-recommendations/` 整個資料夾（輔助腳本＋預覽截圖＋xlsx 副本）、`docs/data-collection/` 下的 `.inspect.ndjson`、`outputs/dev-server/` 的 2 個 log 檔。
- 修復本機 dev server：`.next` 快取因先前多次交錯執行 `next build` 與 dev server 而毀損（`Cannot find module './948.js'`），清除 `.next` 重啟後恢復正常，port 3000 現在正常運作、console 無錯誤。

## 目前資料庫狀態（2026-07-29 查證）

- 卡片總數：**14** 張（原 11 張 ＋ Codex 這批新增 3 張）
- 銀行總數：**9** 家（原 7 家 ＋ 新增台北富邦、星展）
- 優惠總數：**26** 筆
- 卡面配色：**11 張卡已有官網真實色**（DAWHO、CUBE、中信LINE Pay、永豐幣倍卡、台新街口聯名卡、台新Richart卡、玉山Unicard、滙豐旅人系列 3 張、聯邦綠卡）；**3 張新卡（momo卡、滙豐現金回饋御璽卡、星展eco永續卡）尚無配色**，前台走雜湊 fallback，畫面正常不會破版。

## 尚未完成或待決

- **3 個 commit（`f512fdb`～`322049b`）尚未 push**，需與使用者確認後推送到 `hayleyluwei/Credit-card-web`。
- **3 張新卡（momo卡、滙豐現金回饋御璽卡、星展eco永續卡）尚未設定卡面配色**：可比照本輪流程，開瀏覽器截圖官網後填入（後台或直接寫資料庫皆可），非必要、留白也不會破版。
- **多卡面選項的資料模型缺口**（本輪發現、未處理）：玉山Unicard（官方白／黃／藍 3 色可選）、中國信託LINE Pay卡（官網一次 11 款角色卡面輪替），資料庫目前都只能記錄一種代表色，選哪一種、要不要新增欄位記錄「選了哪一款」，尚未拍板，不影響現況運作。
- **後台顏色欄位的「儲存＋前台反映」完整迴圈**：使用者已確認欄位存在於後台表單，但完整存檔測試是否跑過未完全確認，建議之後找機會補測。
- **之後任何指派給 Codex（或其他人）的新資料蒐集任務，需明確指定使用 v4 規格書＋v3 模板**：這個切換不會自動發生，AI／使用者需在下一次任務指示裡明確點出新檔名，否則對方可能還是複製到舊版 v2 模板。
- T18／T20／T22／T24 現況與待決問題**維持 2026-07-27 交接摘要記載不變**，本輪未觸碰，請見該篇。

## 已確認且仍然有效的產品/流程決策（本輪新增）

- T23 卡面視覺方向定案：參數化 SVG 樣板＋官網真實色彩印象（非純演算法雜湊、非官方照片／濾鏡）；卡面顏色欄位存資料庫（非程式碼對照表）；發卡組織標籤不顯示；金色系邊框固定套用、色調跟著晶片走。
- 沿用既有決策：部署平台 Vercel Hobby + Neon Free、增量匯入為預設模式、資料蒐集規格「情境標籤誠實原則」「術語白話原則」等（詳見規格書 v4，內容繼承自 v1-v3）。

## 驗證與環境

- 本輪驗證：`prisma validate`／`prisma db push`／`prisma generate`／`tsc --noEmit`／`eslint`／`next build` 皆通過；瀏覽器實測（含清 DOM 讀取 SVG 漸層色值方式核對，因 Browser 面板偶爾無法截圖）確認卡面顏色、邊框、斷行皆正確；資料庫增量匯入以 dry-run＋真實匯入雙重驗證，並在事後查詢資料庫核對顏色與代號無誤。
- 本機 dev server：本輪對話結束時**運作中**（port 3000，`.next` 已清除重建過，狀態乾淨）。新 session 如果重新整理發現又出現 `.next` 相關錯誤，比照本輪做法：停掉 dev server、`rm -rf .next`、重啟即可。
- Preview／Production 部署狀態：未知（與 2026-07-27 相同，本輪未涉及部署）。

## 新 session 必讀文件

1. `AGENTS.md` → `AI_WORKFLOW_AI協作流程.md` → `CURRENT_STATE_目前專案狀態.md`
2. 本文件
3. `docs/implementation/handoffs/2026-07-27-T16-T24-交接摘要.md`（T18/T20/T22/T24 現況仍以此為準）
4. `docs/implementation/tasks/T23-CARD_VISUAL_STYLE_卡面圖像視覺風格規則.md`（已核准，含完整迭代紀錄）
5. `engineering-data-model-spec/prisma-schema-spec-v5-2026-07-29.md`（schema 現況）
6. `docs/data-collection/DATA_COLLECTION_SPEC_信用卡優惠資料蒐集與分類規格書-v4-2026-07-29.md`（資料蒐集現況）

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD（應為 `322049b` 或更新）、Git status、`.ai-worktree-lock.json`，再讀 `docs/implementation/handoffs/2026-07-29-T23-卡面配色與資料蒐集v4-交接摘要.md` 本文件與 `CURRENT_STATE_目前專案狀態.md`。T23（卡面視覺）本輪已核准並完整實作到 schema v5＋官網真實配色，含後台表單與資料蒐集規格 v4；T18/T20/T22/T24 現況不變，請見 2026-07-27 那篇交接摘要。目前最優先的事：**3 個 commit（`f512fdb`～`322049b`）尚未 push**，需與使用者確認後推送；其餘待辦見上方「尚未完成或待決」。
