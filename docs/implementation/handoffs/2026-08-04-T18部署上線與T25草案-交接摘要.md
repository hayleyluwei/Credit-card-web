# 2026-08-04 交接摘要（T18 部署上線完成、T25 優惠網址穩定性草案）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新）為準，本文件只做導覽。本文件涵蓋的對話**橫跨兩個實際日期**：2026-08-03（T18 schema 遷移／Neon 建立／資料搬遷／Vercel 第一次部署與 `postinstall` 修正）與 2026-08-04（`NEXTAUTH_URL` 修正、正式環境後台登入確認、T18 標記完成、T25 草案建立）——同一輪對話中間有真實時間間隔，commit 時間戳可證實。新 session 若要新增文件，務必先用 `date` 指令核對實際系統日期，不要沿用本文件或對話裡看到的舊日期字串（本輪對話一開始就因為這個原因寫錯過日期，已自行發現並修正，見下方 Git 事實）。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`（工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`，Windows junction，同一份檔案）
- branch：`main`
- 與 origin 關係：本輪對話所有 commit 皆已 push
- 新 session 務必重新執行 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 核對，不得只信本文件

## Git 事實（本輪對話產生的 commit，依時間序）

```
880484f 2026-08-03 11:14:55 +0800  docs(T20): mark guide articles feature complete after final user acceptance
bd6e7c7 2026-08-03 12:18:36 +0800  feat(T18): migrate to PostgreSQL for Vercel/Neon deployment
0958296 2026-08-04 08:14:56 +0800  fix: remove redundant heading card on scenario pages
241598e 2026-08-04 10:27:22 +0800  docs(T18): mark deployment complete, draft T25 URL-stability task card
f0c63da 2026-08-04 10:31:10 +0800  docs: fix mislabeled 2026-08-03 dates on content created after midnight
```

`880484f` 為上一輪交接摘要（`2026-08-03-T20攻略文章實作與收尾-交接摘要.md`）已記錄的內容，此處僅列出供時間軸完整參照。真正屬於本文件的新內容從 `bd6e7c7` 開始。

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本輪對話全程用完即釋放，交接當下不存在。
- 工作區固定存在、與本輪無關、不要動的既有項目（多輪 session 皆未觸碰）：
  - `docs/data-collection/backups/`（歷次備份，持續累積）
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-china-airlines-20260728.xlsx`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-uniopen-20260728.xlsx`

## 本輪完成

**1. T18 第一版部署上線 — 已完成（`bd6e7c7`／`0958296`／`241598e`／`f0c63da`）**：

- **PostgreSQL 遷移**：`prisma/schema.prisma` datasource 改 `postgresql`（schema v8，僅 datasource 一行變更，全部欄位型別本就相容，未調整任何欄位定義）；建立正式 migration `prisma/migrations/20260803033914_init_postgresql/`。
- **Neon 資料庫**：使用者建立 Neon 專案 `credit-card-web`（Singapore region）；唯讀核對本機 `dev.db` 排除 1 筆殘留測試文章（`test0730`）後，用一次性腳本（`scripts/t18-check-local-test-residue.mjs`／`scripts/t18-migrate-local-to-neon.mjs`）把資料原封不動搬過去並重設 PostgreSQL sequence：9 銀行／14 卡／6 分類／26 優惠／30 優惠卡片對應／6 通路／28 RewardTier。
- **正式管理員帳號**：把搬過去的佔位帳號（`admin@example.com`）換成使用者真實帳號 `hayleylu0902@gmail.com`，密碼隨機產生、只在對話中顯示一次、未寫入任何檔案。
- **Vercel 部署**：使用者建立 Vercel 專案 `credit-card-web`，連結 GitHub `main` 分支，正式網址 `https://credit-card-web-pi.vercel.app`。第一次部署失敗（`PrismaClientInitializationError`，Vercel 依賴快取導致 `prisma generate` 未觸發），已加 `package.json` 的 `"postinstall": "prisma generate"` 修正。`NEXTAUTH_URL` 一開始填錯猜測網址，08-04 修正並手動 Redeploy 後生效。
- **上線後驗證全數通過**：自動驗證（`prisma validate`／`tsc`／`data:verify-release`／`next build`）與正式網址人工＋瀏覽器驗證（首頁、六個分類頁、搜尋、三個優惠詳情頁、銀行頁、卡片頁）皆正常；**使用者本人已在正式網址親自登入後台成功**，Dashboard 資料正確（26 已發布優惠／14 卡／9 銀行）。
- Summary：`docs/implementation/summaries/T18-FIRST_RELEASE_DEPLOYMENT_SUMMARY-v1-2026-08-04.md`。
- 已知風險（非阻擋）：Neon Free／Vercel Hobby 有冷啟動延遲，已確認為免費方案預期行為。

**2. 測試期間的 ad-hoc UX 清理（非既有任務卡，對話中即時核准）**：
- `/scenarios/[slug]` 移除重複的「符合「X」情境的優惠」標題卡片（跟頁面自己的 H1／說明重複），`0958296`。
- `/banks/[slug]` 移除「本行相關卡片」「相關優惠」下方重複說明文字；`/cards/[slug]` 移除「相關優惠」下方重複說明文字（皆為純裝飾、後台無對應可編輯欄位，同一類已在 T20 清過的側欄問題），`241598e`。

**3. T25（優惠網址穩定性與過期轉址）v1 草案，待核准**：使用者在後台編輯 `cube-tiered-category-rewards-2026` 時發現資料蒐集規格書 3.4「情況二」換代號的設計缺口——優惠過期後前台會直接 404，不會轉址到新版本，長期會流失 SEO 收錄與排名。已建任務卡 `docs/implementation/tasks/T25-OFFER_URL_STABILITY_優惠網址穩定性與過期轉址.md` 記錄問題與可能方向（新增「取代關係」欄位＋過期時 301 轉址），5 個待決問題 (a)–(e) 未拍板，只是規劃文件，不構成實作授權。不影響現有資料（多屬「情況一」延續，尚未真的發生過情況二），不擋 T18 上線。

**4. 本輪對話發現並自行修正的流程問題**：文件裡把 2026-08-04 發生的事寫成 2026-08-03（同一種「對話跨日、日期字串沒重新核對」的錯誤，上一輪對話的 `65e3b78` commit 也修過一次同類問題）。已用 `f0c63da` 修正 T18 Summary（含檔名重新命名）、T25 任務卡、任務索引、CURRENT_STATE 的日期字串。

## 尚未完成或阻塞

- **T25 尚未核准**：5 個待決問題 (a)–(e) 待使用者拍板，核准前不得修改 schema 或程式碼。
- **T23 補測未執行**：後台顏色欄位「儲存＋前台反映」完整迴圈測試腳本已建立但使用者尚未跑過。
- T22（排程輔助資料更新）、T24（申辦導引連結）：草案階段，未變動。
- GA 網站流量分析：待另開小任務。

## 已確認且仍然有效的產品/流程決策

- T18 部署平台：Vercel Hobby（`credit-card-web` 專案，網址 `credit-card-web-pi.vercel.app`）＋ Neon Free（`credit-card-web` 專案，Singapore region）。
- 正式環境環境變數：`DATABASE_URL`／`NEXTAUTH_SECRET` 設為 Sensitive；`NEXTAUTH_URL` 設為 `https://credit-card-web-pi.vercel.app`。`ADMIN_EMAIL`／`ADMIN_PASSWORD` 正式環境不需要（只有本機 `db:seed` 會讀，正式環境不跑 seed，登入邏輯查 `AdminUser` 資料表）。
- `package.json` 新增 `"postinstall": "prisma generate"`，往後每次 Vercel 部署都會強制重新產生 Prisma Client，不受依賴快取影響——這是必要設定，不要移除。
- 正式管理員帳號：`hayleylu0902@gmail.com`（密碼使用者自行保管，AI 未留底）。
- 純裝飾、無對應可編輯欄位、只是重複頁面標題或說明的靜態文字，比照本輪與 T20 已清過的側欄慣例，發現就可以直接拿掉，不需要另外走產品規格流程確認（純 UI 精簡，不影響資料或功能）。
- Repo 是 **public**，任何腳本或文件不得寫死使用者真實 email／密碼／連線字串；`.env` 已正確 `.gitignore`。

## 驗證與環境

- 自動驗證：PostgreSQL 連線下 `prisma validate`／`tsc --noEmit`／`data:verify-release`／`next build`（34 路由，14 情境頁 SSG）皆通過。
- 正式環境人工驗證：首頁、六個分類頁、搜尋、三個優惠詳情頁、銀行頁、卡片頁、後台登入（使用者本人操作）皆確認正常。
- Preview／Production：**已部署，已驗證**，網址 `https://credit-card-web-pi.vercel.app`；截至 `f0c63da` 為最新已部署 commit（push 會自動觸發 Vercel 部署，新 session 若有新 commit 建議到 Vercel Deployments 分頁核對最新一筆對應的 commit）。
- 本機 dev server：交接當下已停止；新 session 需要時自行用 preview 工具重開，注意本機 `.env` 的 `DATABASE_URL` 現在指向 Neon（正式資料庫），本機開發即是直接連正式資料庫，寫入測試資料前要特別小心，不要污染正式資料。

## 下一步

**沒有唯一「最優先」項目，以下依可能性排序，實際看使用者想先討論哪個**：
1. T25（優惠網址穩定性與過期轉址）：使用者最近提出、AI 已建草案，5 個待決問題待拍板，是最新鮮的討論題目。
2. T23 補測：後台顏色欄位儲存迴圈，使用者有空登入操作即可。
3. T22／T24：仍是草案，待使用者決定是否核准開工。
4. GA 網站流量分析：待另開小任務。

## 新 session 必讀文件

1. `AGENTS.md` → `AI_WORKFLOW_AI協作流程.md` → `CURRENT_STATE_目前專案狀態.md`
2. 本文件
3. 若接續 T25：`docs/implementation/tasks/T25-OFFER_URL_STABILITY_優惠網址穩定性與過期轉址.md`（v1 草案，待核准，5 個待決問題）
4. 若要看 T18 完整過程：`docs/implementation/summaries/T18-FIRST_RELEASE_DEPLOYMENT_SUMMARY-v1-2026-08-04.md`

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD（應為 `f0c63da` 或更新）、Git status、`.ai-worktree-lock.json`，再讀 `docs/implementation/handoffs/2026-08-04-T18部署上線與T25草案-交接摘要.md` 本文件與 `CURRENT_STATE_目前專案狀態.md`。本輪完成：T18 第一版部署上線已正式標記完成，正式站 `https://credit-card-web-pi.vercel.app` 已上線並驗證（含使用者親自登入後台成功）；同輪對話也建立了 T25（優惠網址穩定性與過期轉址）v1 草案，待核准，5 個待決問題未拍板。接下來想討論 T25 待決問題、還是先處理別的（T23 補測／T22／T24），請看上方「下一步」與待決事項自行選擇或詢問使用者。
