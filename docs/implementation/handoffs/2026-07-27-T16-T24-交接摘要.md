# 2026-07-27 交接摘要（T16-T21 收尾＋T18/T20/T22-T24 規劃＋T23 原型）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新）為準，本文件只做導覽，不重複列出全部細節。本文件涵蓋的對話範圍非常長，取代先前的 `2026-07-26-T16-T19-T21-交接摘要.md`（該篇仍保留供歷史追溯）。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`（工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`，Windows junction，同一份檔案）
- branch：`main`，HEAD：`efcc213`
- 與 origin 關係：`origin/main` 在 `ae369ec`，**本地領先 7 個 commit 尚未推送**（`ce0dd72`～`efcc213`，內容見下方「本輪完成」）
- 新 session 務必重新執行 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 核對，不得只信本文件

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本文件建立時**不存在**（已釋放）。本次對話中曾與 Codex 交替持有：Codex 完成一批中國信託 LINE Pay 卡資料匯入後釋放，Claude 接手做完文件/程式工作後也都有釋放。新 session 開始時仍要重新檢查一次是否存在、是否為 Codex 持有。
- 已知長期未提交變更（T15 治理文件批次、首頁舊工作等）**已於本輪全部 commit 並推送**，不再是待處理項。
- 目前工作區僅剩 **Codex 留下的未追蹤資料檔案**（不屬本文件範圍，不要動）：
  - `docs/data-collection/backups/`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-china-airlines-20260728.xlsx`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-uniopen-20260728.xlsx`
  - `outputs/dev-server/localhost-3000-std{out,err}.log`

## 目前任務線總覽（詳見 CURRENT_STATE）

| 任務 | 狀態 |
|---|---|
| T15/T16/T17/T19 | ✅ 完成 |
| **T21**（優惠條件結構化，重構 Offer→RewardTier/Channel） | ✅ **完成**（2026-07-27）——schema 已到 v4，扁平回饋欄位已移除；四層驗證證據齊備（自動驗證＋資料層測試腳本＋真實多層資料＋使用者親自後台實測） |
| **T18**（部署上線，Vercel Hobby + Neon Free） | 待核准，已核准的部署平台方案不變；卡片內容已更新到反映 T21/schema v4 現況 |
| **T20**（攻略文章功能） | 草案待核准，已補充「水電瓦斯搜尋情境測試」佐證 |
| **T22**（排程輔助資料更新，半自動） | 草案待核准，5 個待決問題未拍板 |
| **T23**（卡面圖像視覺風格） | 草案待核准，**已選定「委託簡化插畫」方向，已建原型並發布，等使用者對原型回饋** |
| **T24**（信用卡申辦導引連結） | 草案待核准，**v1 方向已拍板**：純導引官網申辦頁＋UTM，不含聯盟行銷 |

## 本輪完成（依時間序，含 commit hash）

**T16/T19 收尾**：`3bbc7ca` 建 T16 Summary、任務索引與 CURRENT_STATE 正式標記完成。

**T17 執行**：`d47ce6e` 建 `scripts/verify-release-data.mjs`（唯讀）、盤點並停用 3 支舊 seed-only smoke 腳本、建人工測試腳本與 16 筆優惠資料抽查清單（AI 用 WebFetch 逐筆比對官網，發現 CUBE 卡疑似舊快照＋兩筆永豐優惠疑似期間未同步，事後皆已由使用者確認/我方修正解決）。使用者最終拍板「當作完成，往下走」。

**增量匯入模式**：`0a7e597` 把 `import-offer-data.mjs` 從「整批清空重建」改為「slug 比對、增量 upsert」為預設，`--reset` 保留備用；資料蒐集規格升版 v3，新增「半年更新情況一（延展沿用代號）vs 情況二（換新代號）」判斷規則。

**資料修正**：`5cbbd23` 依規格 v3 情況一，把永豐兩筆優惠（稅款分期／學費回饋）從已過期的上半年日期同步到官網現行的下半年，同時改了 xlsx 源頭（不只是資料庫）。

**長期未提交批次全部收尾**：`129808c`（首頁「選你手上的信用卡」功能）、`290aa00`（DAWHO seed 真實文字改寫）、`0fbe5b1`（T15 治理框架整批：AGENTS.md 重寫、SOP、任務卡等 30 檔）。

**T21 全五階段**（優惠條件結構化，使用者核准後直接動 schema）：
- `9ea9905` Phase 1-3：schema 加 `RewardTier`/`Channel`/`RewardTierChannel`（v3，扁平欄位暫留當橋樑）、資料遷移腳本（16 offer→16 tier）、前台改讀 tiers
- `b5ac497` Phase 4-5a：後台動態「回饋層」表單、`admin-actions.ts`／`domain-validation.ts` 改用 tiers、匯入腳本與 seed 改寫
- `9fe4e9d` Phase 5b：**移除 Offer 扁平回饋欄位**（schema v4），前台 fallback 一併清除；`prisma validate`／`tsc`／`next build` 全通過
- `e15428c` 新增資料層測試腳本 `scripts/verify-t21-tier-data-layer.mjs`（11 項斷言全過，驗證 tier 建立/整批替換/刪除邏輯）
- `5d7ef61` **使用者親自登入後台**，在 Codex 匯入的中國信託 LINE Pay 卡（本身即真實 3 層資料）新增第 4 層測試 tier、前台正確反映、再自行刪除——T21 正式標記完成

**治理與文件**：`943930c`（T17 標完成、T21 核准記錄、T22 草案）、`f259a0e`（T20 補搜尋情境測試佐證）、`ce0dd72`＋`240e00d`（新建 `docs/data-collection/OFFICIAL_SOURCES_官方來源與輔助網址參考.md`，記錄各銀行官方來源＋輔助頁＋部落格線索＋已觀察更新規律，並註明「僅收錄已知的，非全部，仍需主動搜尋」）、`4aacd4d`（T18 任務卡更新到反映 T21/schema v4 現況）、`e08a6cb`＋`efcc213`（起草 T23/T24 並記錄拍板決策）。

**公開頁清理**：`cc83cfc`（移除卡片頁外露的內部欄位「是否啟用」「SEO 標題」）、`0a0af1e`（移除優惠詳情頁外露的「推薦分數」）。

**新功能**：`694e4b4`（`/search` 加熱門搜尋預設詞：現金回饋/旅遊/繳稅/學費/水電瓦斯/分期）、`ae369ec`（新建 `/cards` 全卡片列表頁＋首頁「查看全部」連結；「目標客群」全站改名「適用對象」）。

**T23 原型（未 commit，非 repo 一部分）**：用 4 張真實卡片（涵蓋乾淨值/英文 tier/複合雜亂字串/全 null 四種資料型態）產生參數化 SVG 卡面原型，發布為 Artifact（`https://claude.ai/code/artifact/01828680-4a8d-404c-a2af-bb2fc860a073`），**等待使用者對視覺方向的回饋**，尚未動任何正式程式碼。

## 尚未完成或待決

- **T23 原型待使用者回饋**：喜不喜歡這個「色塊＋字母＋文字標籤＋金色 tier 裝飾線」的方向；回饋後才決定是否寫進正式 Scope 並核准實作（含要不要真的接進 `CardImage.tsx`）。
- **T18 待正式核准開工**：Vercel/Neon 帳號需使用者自行申請（AI 不能代為建立帳號）；已討論過「若考慮下廣告，Vercel Hobby 禁止商業用途，Railway/Render 無此限制」。
- **T22 五個待決問題未拍板**：排程機制(a)、抓取方式(b)、頻率(c)、與 T21 排序(d)、報告自動化程度(e)。
- **T20 仍待核准**（不影響上線主線）。
- **T24 尚未正式核准 Scope**，但 v1 方向已定：純導引＋UTM，不含聯盟行銷。
- **GA 網站流量分析**：使用者已決定排在 T18 上線後另開小任務，不佔用 T18 Scope。
- 這批 7 個 commit（`ce0dd72`～`efcc213`）**尚未推送**，需使用者確認後才 push。

## 已確認且仍然有效的產品/流程決策

- 部署平台 Vercel Hobby + Neon Free（2026-07-08 拍板，未變）；後台帳號使用者本人持有。
- T21 扁平欄位**最終移除**（非保留過渡），已完整執行（Phase 5b）。
- 資料蒐集規格 v3：半年更新「情況一/情況二」判斷規則、增量匯入為預設模式。
- T24 v1：純導引官網申辦頁＋UTM，不含聯盟行銷（聯盟行銷列未來獨立決策，會牽動部署平台選擇）。
- GitHub push 授權：本輪多次 push 到 `hayleyluwei/Credit-card-web` 已是使用者知情同意的既成事實（已寫入 T18 任務卡待決問題）。

## 驗證與環境

- T21 驗證：`prisma validate`／`tsc --noEmit`／`npx next build` 皆通過；資料層測試腳本 11 斷言全過；使用者親自後台實測通過。
- 本機 dev server：對話中多次開關（`.claude/launch.json` 設定 `npm run dev`，port 3000），結束時可能已停止，新 session 用 preview 工具重開即可；曾遇過 `.next` 快取因交錯執行 `next build` 與 dev server 而毀損，清 `.next` 重啟即解決。
- schema 現況：`prisma/schema.prisma` 為 **v4**（`RewardTier`／`Channel`／`RewardTierChannel` 三個新 model，`Offer` 扁平回饋欄位已移除），對應規格說明在 `engineering-data-model-spec/prisma-schema-spec-v4-2026-07-27.md`，版本備份 `schema-v3-2026-07-27.prisma`／`schema-v4-2026-07-27.prisma`。

## 新 session 必讀文件

1. `AGENTS.md` → `AI_WORKFLOW_AI協作流程.md` → `CURRENT_STATE_目前專案狀態.md`
2. 本文件
3. `docs/implementation/tasks/T18-FIRST_RELEASE_DEPLOYMENT_第一版部署上線.md`（待核准，內容已更新到反映 T21）
4. `docs/implementation/tasks/T21-CONDITION_SCHEMA_優惠條件結構化規劃.md`＋`summaries/T21-CONDITION_SCHEMA_SUMMARY-v1-2026-07-27.md`（已完成）
5. `docs/implementation/tasks/T23-CARD_VISUAL_STYLE_卡面圖像視覺風格規則.md`（原型狀態）
6. `docs/implementation/tasks/T24-APPLY_LINKS_信用卡申辦導引連結.md`
7. `docs/data-collection/OFFICIAL_SOURCES_官方來源與輔助網址參考.md`（資料更新查詢起點）
8. `docs/data-collection/DATA_COLLECTION_SPEC_信用卡優惠資料蒐集與分類規格書-v3-2026-07-27.md`

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD（應為 `efcc213` 或更新）、Git status、`.ai-worktree-lock.json`（可能由 Codex 持有，寫入前要確認是否仍有效），再讀 `docs/implementation/handoffs/2026-07-27-T16-T24-交接摘要.md` 本文件與 `CURRENT_STATE_目前專案狀態.md`。T15/T16/T17/T19/T21 皆已完成；T18/T20/T22/T23/T24 皆待核准或待使用者回饋。目前最優先的兩件事：① 本地有 7 個 commit（`ce0dd72`～`efcc213`）尚未推送，需與使用者確認後 push；② T23 已建好卡面視覺原型（Artifact 連結見本文件），等待使用者回饋以決定下一步。
