# 2026-08-03 交接摘要（T20 攻略文章功能與自動情境頁：實作、人工驗收、收尾修正）

用途：本次對話結束前的交接快照。詳細狀態一律以 `CURRENT_STATE_目前專案狀態.md`（已同步更新）為準，本文件只做導覽。本文件涵蓋的對話**橫跨兩個實際日期**：2026-07-30（T20 核准、schema v6/v7、完整實作、首頁 UX 收尾）與 2026-08-03（使用者人工測試 T20、回報並修正兩個問題）——同一輪對話中間有真實時間間隔，commit 時間戳可證實（見下方 Git 事實）。新 session 若要新增文件，務必先用 `date` 指令核對實際系統日期，不要沿用本文件或對話裡看到的舊日期字串。

## 專案定位

- Git root：`C:/Users/user/Documents/Credit card web project`（工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`，Windows junction，同一份檔案）
- branch：`main`
- 與 origin 關係：本輪對話所有 commit 皆已 push（詳見下方 Git 事實）
- 新 session 務必重新執行 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 核對，不得只信本文件

## Git 事實（本輪對話產生的 commit，依時間序）

```
3fd8e4d 2026-07-30 08:44:56 +0800  docs(T20): draft auto scenario pages, expand core scenario tags to 14
81c3ba7 2026-07-30 09:03:23 +0800  docs: fix mislabeled dates on content created after midnight (7/29 -> 7/30)
9e75c95 2026-07-30 09:49:33 +0800  feat(T20): add guide articles and auto scenario pages
cf073a3 2026-07-30 10:21:03 +0800  feat: replace redundant offer badge with editable label, add homepage icons
25e2582 2026-07-30 10:45:40 +0800  docs+fix: homepage nav placement, category sidebar cleanup, offer badge tidy
65b350b 2026-07-30 10:46:08 +0800  docs: update CURRENT_STATE HEAD reference to latest commit
31af704 2026-08-03 10:55:09 +0800  fix(T20): graceful article form errors, add delete with confirm
```

`3fd8e4d`／`81c3ba7` 為上一輪交接摘要（`2026-07-30-T20情境頁與訂閱標籤-交接摘要.md`）已記錄的內容，此處僅列出供時間軸完整參照。真正屬於本文件的新內容從 `9e75c95` 開始。

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本輪對話全程用完即釋放，交接當下不存在。
- 工作區固定存在、與本輪無關、不要動的既有項目（多輪 session 皆未觸碰）：
  - `docs/data-collection/backups/`（歷次備份，持續累積）
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-china-airlines-20260728.xlsx`
  - `docs/data-collection/信用卡優惠資料整理模板-v2-2026-07-18-ctbc-uniopen-20260728.xlsx`

## 本輪完成

**1. T20 Scope v3 正式核准（2026-07-30）**，使用者於對話中確認：Markdown 套件用 `react-markdown`＋`remark-gfm`；Git 授權為 `git add`＋local commit（不含 push）。

**2. T20 完整實作（commit `9e75c95`）**：
- schema 依專門流程升版至 **v6**：新增 `Article` model（`title`／`slug`／`summary`／`contentMd`／`seoTitle`／`seoDescription`／`faqJson`／`lastVerifiedAt`／`isPublished`／`publishedAt`）；情境標籤對照表維持程式碼設定檔（`src/lib/domain-scenarios.ts`），未進資料庫。
- `/scenarios/[slug]`：14 個核心情境標籤各一個靜態頁（`generateStaticParams`），比照 `/categories/[slug]`，含 `WebPage` JSON-LD、與同 slug 攻略文章的交叉連結。
- `/guides`、`/guides/[slug]`：Markdown 渲染（預設不解析 raw HTML，防儲存型 XSS，已用 `<script>alert('xss')</script>` 實測確認未執行）、FAQ、`Article`／`BreadcrumbList`／`FAQPage` JSON-LD。
- `/admin/articles`、`/admin/articles/[id]`：文章後台 CRUD 起始版本。
- `sitemap.ts`、`public/llms.txt`、首頁情境/攻略入口、`docs/sop/GUIDE_ARTICLE_WRITING_攻略文章撰寫準則.md`。
- 自動驗證：`prisma validate`／`tsc --noEmit`／`eslint`／`next build`（34 路由，14 情境頁 SSG）全過。

**3. Ad-hoc UX 收尾（非既有任務卡，對話中即時核准，commit `cf073a3`／`25e2582`）**：
- `Offer.badgeLabel`（schema **v7**）取代恆真的「進行中／已過期」徽章，行銷可自訂文字，留空不顯示。
- 首頁新增 `src/components/EntryIcon.tsx`（手刻 inline SVG，未新增套件相依），情境標籤與分類卡片換成一致風格圖示；首頁區塊重排為「熱門情境 → 熱門優惠分類 → 信用卡優惠 → 精選優惠 → 最新優惠」；導覽按鈕移出 hero 表頭。
- `/categories/[slug]` 移除「分類說明」／「常見問題」視覺重複側欄（`FAQPage` JSON-LD 結構化資料保留）。
- `OfferCard` 不再顯示「一般」字樣，只在 `isFeatured` 為真時顯示「精選」。

**4. 使用者人工測試 T20（2026-08-03，同一輪對話跨日繼續）**：完整跑完人工測試腳本 8 項（登入、新增、編輯、發布、前台可見、下架 404、刪除、Slug 修改提醒），過程中發現並回報兩個問題，AI 已修正（commit `31af704`）：
- **FAQ JSON／Slug 錯誤會讓整頁崩潰**：`createArticle`／`updateArticle` 原本用 `throw new Error(...)`，Server Action 未捕捉例外，導致 Next.js 開發模式紅色錯誤畫面。已改為 `AdminActionState` 錯誤回傳模式（新增 `src/components/AdminArticleForm.tsx`，client component＋`useFormState`），驗證失敗改為表單內紅色提示框，不再整頁崩潰。
- **後台原本無刪除文章功能**：已新增 `deleteArticle` server action＋`src/components/ConfirmSubmitButton.tsx`（帶瀏覽器 `confirm()` 二次確認的刪除按鈕），`/admin/articles` 列表頁每篇文章可刪除。`Article` model 未被其他 model 參照，刪除安全。

**5. 修正過程中的操作插曲**：AI 在 dev server 執行中又多跑一次 `next build`，兩者搶同一份 `.next` 快取導致損毀（`Cannot find module './948.js'`）。已停止 dev server、清除 `.next`（純建置快取，已列入 `.gitignore`，不影響原始碼或資料）、重啟乾淨排除。

**6. 文件同步**：`CURRENT_STATE_目前專案狀態.md`、`01-ACTIVE_TASK_INDEX_目前任務索引.md`、T18／T20 任務卡核准紀錄、T20 Summary（`summaries/T20-GUIDE_ARTICLES_SUMMARY-v1-2026-07-30.md`）、T20／T23 人工測試腳本皆已更新；schema v6／v7 規格書與修正清單已產出（`engineering-data-model-spec/`）。

**7. 順手處理的治理項目（2026-07-30，同輪對話較早段落）**：
- T21 排序問題（T16→T17→T21→T18）確認已於 2026-07-18 拍板且已依此順序執行完成，修正 `CURRENT_STATE` 殘留的過時「待決」文字。
- T18 Scope v1 正式核准；記錄 Vercel/Neon 帳號使用者已申請完成，外部阻塞解除。
- T23 補測腳本建立：`docs/implementation/manual-test-scripts/T23-後台顏色欄位儲存與前台反映測試腳本-v1-2026-07-30.md`（**尚未執行**，見下方待辦）。

## 尚未完成或阻塞

- **T20 最後一哩路**：新增的「刪除」按鈕＋確認提示、修正後的 FAQ／Slug 錯誤訊息顯示，**尚未經使用者實測確認**（只有 AI 用 `tsc`／`eslint`／`next build` 驗證過程式碼正確，未實際點擊登入操作）。確認後人工測試腳本才算全數通過，T20 才能正式標記「完成」。
- **T23 補測未執行**：後台顏色欄位「儲存＋前台反映」完整迴圈測試腳本已建立但使用者尚未跑過。
- **T18 待使用者於 Vercel/Neon 主控台實際操作**：Scope 已核准，但建立 Neon 資料庫、Vercel 專案、環境變數設定需使用者在各自主控台操作或提供必要資訊，AI 無法代為建立帳號或專案；PostgreSQL schema 遷移（現行 v7）AI 可協助執行。
- 情境頁與攻略文章的關鍵字互搶風險緩解方式：本輪先採「slug 相同即視為對應」的交叉連結分工，實際上線後成效待觀察，任務卡列為風險項未定案。
- 新增的 5 個情境標籤（電影／高鐵台鐵／eTag／停車／道路救援）目前資料庫裡沒有任何優惠掛這些標籤，規格已定義、資料待補。
- T22（排程輔助資料更新）、T24（申辦導引連結，v1 方向已拍板但仍待正式核准）：草案階段，未變動。

## 已確認且仍然有效的產品/流程決策

- T20 v3：自動情境頁（`/scenarios/[slug]`）與人工攻略文章（`/guides/[slug]`）並行，14 個核心情境標籤，情境標籤對照表寫死在程式碼、不開後台管理。
- Markdown 渲染只用 `react-markdown`＋`remark-gfm`，不解析 raw HTML；未另外安裝排版套件（`@tailwindcss/typography` 等），改用自訂 `components` 覆寫樣式。
- 優惠卡片右上角徽章（`Offer.badgeLabel`）由行銷自訂文字取代系統計算的「進行中/已過期」；卡片右下角只在 `isFeatured` 時顯示「精選」，不顯示「一般」。
- `/categories/[slug]` 不再顯示「分類說明」／「常見問題」視覺側欄，結構化資料（`FAQPage` JSON-LD）維持。
- 首頁固定順序：熱門情境 → 熱門優惠分類 → 信用卡優惠 → 精選優惠 → 最新優惠；導覽按鈕（搜尋優惠／瀏覽分類／攻略文章）獨立一排，不放在 hero 表頭內。
- 後台文章管理提供刪除功能（帶確認提示），與其他內容類型（銀行／卡片／分類／優惠）只有停用/發布切換、無刪除的既有慣例不同——這是使用者針對 `Article` 明確要求的例外，非全站規則變更。
- AI 不會、也不應代為輸入管理員密碼；後台功能的實際點擊驗證固定由使用者本人操作，AI 負責事後用瀏覽器唯讀核對前台結果。

## 驗證與環境

- 自動驗證：`npx prisma validate`／`npx prisma db push`／`npx tsc --noEmit`／`npx eslint`（異動檔案）／`npx next build`（34 路由）皆於本輪多次執行並通過，最後一次在 `31af704` 之前執行過。
- 瀏覽器唯讀驗證：情境頁篩選、空狀態、JSON-LD、sitemap 條目、XSS 防護（`<script>` 未執行）、首頁圖示與順序、分類頁單欄版面、優惠卡片徽章顯示，皆已用 `mcp__Claude_Browser__*` 工具核對過。
- 人工測試腳本：`docs/implementation/manual-test-scripts/T20-攻略文章與自動情境頁測試腳本-v1-2026-07-30.md`（8 項已由使用者跑完，2 個問題已修正但修正本身待確認）、`docs/implementation/manual-test-scripts/T23-後台顏色欄位儲存與前台反映測試腳本-v1-2026-07-30.md`（尚未執行）。
- 本機 dev server：本輪對話中曾因 AI 誤操作（dev server 執行中又跑 `next build`）損毀 `.next` 快取，已排除；交接當下 dev server 狀態未知（對話結束前可能已停止），新 session 需要時自行用 preview 工具重開。
- Preview／Production 部署狀態：未部署，未知。

## 下一步

**最優先且範圍明確的下一步**：使用者登入後台，確認新增的「刪除」按鈕（含確認提示）與修正後的 FAQ／Slug 錯誤訊息顯示是否符合預期。確認通過後，T20 人工測試腳本可正式標記全數通過，任務狀態改為「完成」。

其餘依優先順序（不在本次交接中同時開工）：
1. T20 正式標記完成後，更新任務索引與 CURRENT_STATE。
2. T23 補測（後台顏色欄位儲存迴圈）——使用者有空時登入操作。
3. T18（部署上線）——使用者於 Vercel/Neon 主控台建立實際專案；AI 可協助 PostgreSQL schema 遷移與部署前檢查。
4. T22／T24 仍為草案，待使用者決定是否核准開工。

## 新 session 必讀文件

1. `AGENTS.md` → `AI_WORKFLOW_AI協作流程.md` → `CURRENT_STATE_目前專案狀態.md`
2. 本文件
3. `docs/implementation/tasks/T20-GUIDE_ARTICLES_攻略文章功能.md`（v3 已核准）
4. `docs/implementation/summaries/T20-GUIDE_ARTICLES_SUMMARY-v1-2026-07-30.md`（含附錄記錄 ad-hoc UX 修正與人工測試發現的問題）
5. `docs/implementation/manual-test-scripts/T20-攻略文章與自動情境頁測試腳本-v1-2026-07-30.md`（含「執行結果」段落）
6. 若接續 T18：`docs/implementation/tasks/T18-FIRST_RELEASE_DEPLOYMENT_第一版部署上線.md`

## 可直接貼入新對話的開場文字

請依 `AGENTS.md` 的專案啟動流程接續目前工作。先核對 Git root、branch、HEAD（應為 `31af704` 或更新）、Git status、`.ai-worktree-lock.json`，再讀 `docs/implementation/handoffs/2026-08-03-T20攻略文章實作與收尾-交接摘要.md` 本文件與 `CURRENT_STATE_目前專案狀態.md`。本輪完成：T20 攻略文章功能與自動情境頁完整實作並經使用者人工測試 8 項全過，過程中發現的 FAQ／Slug 崩潰問題與缺少刪除功能已修正（commit `31af704`），但修正本身尚待使用者實測確認；同輪對話也做了優惠徽章改自訂文字、首頁圖示與版面收尾等 ad-hoc UX 修正。其餘待辦與待決項目見上方「尚未完成或阻塞」。
