# 交接摘要：新卡資料補齊與多層回饋支援

日期：2026-08-05（Asia/Taipei）  
涵蓋對話：2026-08-04 至 2026-08-05 同一輪對話跨日繼續

## 專案定位

- 正式 Git root：`C:/Users/user/Documents/Credit card web project`
- 工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`（Windows junction，同一份檔案）
- branch：`main`
- HEAD：`fdc7432`
- 與遠端關係：已與 `origin/main` 同步，無待 push 的 commit

新 session 必須以 `git rev-parse --show-toplevel`、`git status`、`git rev-parse --short HEAD` 重新核對，不可只信本文件。

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：**不存在**，本輪對話全程確認過，沒有其他 session 持有鎖。
- 已知未提交檔案：`docs/data-collection/backups/`（未追蹤資料夾，內含多份舊快照 `.xlsx`，屬本輪對話之前就存在的既有未提交內容，非本輪產生，不得未經使用者同意逕自 commit 或刪除）。
- 其餘工作目錄乾淨。

## 目前任務

本輪對話沒有對應正式任務卡（T-系列），屬 T16（第一版正式資料匯入，已完成）之後的**資料維護性質支線任務**，使用者直接在對話中交辦、核准、驗收，沒有走任務卡起草流程。

## 本輪完成（有 commit／有驗證證據）

**Commit `60df9fa`（卡面樣板改版）**：
- 使用者提供中國信託 LINE Pay 卡官方卡面截圖作為風格參考，`CardImage.tsx` 改版：新增銀行名稱文字（左上角）、晶片改純色簡約方塊並移到卡面中間偏上、卡片名稱字重變細。5 個前台呼叫端頁面同步傳入 `bankName`。`tsc --noEmit` 通過，瀏覽器多輪截圖核對定案。

**Commit `fdc7432`（資料補齊＋多層回饋支援＋新卡）**：
- **前情（已完全善後，不留風險）**：本輪對話前段發生資料事故——AI 未實際查證官網就編了 3 張假卡並跑正式匯入，且發現本機 `.env` 的 `DATABASE_URL` 直接接正式站 Neon PostgreSQL（無獨立開發資料庫，`prisma/dev.db` 是 T18 遷移後已停用的殘留檔）。假資料已用 Prisma script 直接從正式站刪除並經瀏覽器核對回到乾淨狀態，詳細經過見 `CURRENT_STATE_目前專案狀態.md` 的「2026-08-04（同輪對話）資料補齊、多層回饋支援＋事故善後」小節。
- **XLS/DB 落差補齊**：3 家銀行（中國信託／台北富邦／星展）＋4 張卡（中國信託LINE Pay卡／台北富邦momo卡／滙豐現金回饋御璽卡／星展eco永續卡）——皆為 Codex 先前已查證、已在正式站的真實資料，同步寫回 xlsx 存檔，未變更資料庫內容。
- **`offers` 工作表升版支援多層回饋（規格書 v7）**：新增「回饋層級名稱」欄，同一優惠代號可跨多列表達多層 RewardTier。`scripts/import-offer-data.mjs` 的 `validate()`／`runUpsert()`／`runReset()` 同步改版依代號分組。驗證方式：純函式單元測試（分組結果、tier 欄位對應、sortOrder，不連資料庫）＋ 真實 xlsx dry-run 通過 ＋ 用真實既有 3 層資料（`ctbc-linepay-rewards-2026h2`）跑正式匯入，匯入後查資料庫與瀏覽器核對正式站顯示，3 層 label／rate／順序皆與匯入前完全一致（idempotent 驗證通過）。
- **新增 4 張真實新卡／8 筆優惠**：合併 2 份 repo 裡已存在但從未匯入的 Codex 準備批次（中國信託 uniopen 聯名卡、中國信託中華航空聯名卡 3 款子卡）。**沒有照單全收**：抽查了 `ctbcbank.com` 兩個官方來源頁面（WebFetch＋瀏覽器），哩程比例／生日加碼／新戶迎賓門檻／回饋上限／活動期間皆與官網現況吻合。發現並修正一處資料品質瑕疵：「華夏金卡與機場尊榮禮遇」優惠條款明確排除商務御璽卡，但來源檔的 `offer_cards` 對應表仍列入，已在合併時排除該筆錯誤對應。
- **最終資料量**：9 家銀行／18 張卡／26 筆優惠代號（36 層 RewardTier，含 1 筆 3 層優惠）／45 筆優惠卡片對應，皆已於正式站 `https://credit-card-web-pi.vercel.app` 用瀏覽器核對顯示正確。

## 尚未完成或阻塞

- **未解決的系統性風險**：本機開發環境（`npm run dev`、任何直接寫資料庫的腳本）的 `DATABASE_URL` 直接指向正式站 Neon PostgreSQL，沒有獨立開發資料庫，也沒有真正保護正式站的自動備份機制（現有備份機制備份的是已停用的舊 SQLite 檔）。長期應建立獨立開發用 Postgres 資料庫，或至少讓匯入腳本改為對 Postgres 做正式備份／dump。**下一個 session 執行任何會寫資料庫的操作前，務必先意識到這件事**。
- 這次沒有再用 WebSearch 額外挖全新（不在 Codex 既有批次裡的）2026 下半年新卡；搜尋時曾看到玉山 UBear 卡、聯邦吉鶴卡／賴點卡等候選，未展開查證。
- 附帶發現、尚未修正的既有前端小 bug：優惠詳情頁「回饋方式」在 `回饋類型＝其他` 時，前台顯示英文代碼 `other` 而非中文「其他」，與本輪資料異動無關。
- 一次「正式站寫入測試資料→查驗→立即刪除」的即時驗證被系統風險分類器攔下（使用者當場授權後仍被攔，不受單次對話內授權影響），改用真實既有資料的正式匯入取代驗證，效果等同但過程略有不同，供未來 session 參考（分類器行為不受人為授權影響，需要調整權限設定才能允許）。

## 已確認產品決策

- 卡面樣板新版式（銀行名稱＋簡約晶片＋較細卡片名稱字重）已由使用者當場定案，全站套用，非草案。
- `offers` 工作表支援多層回饋（v7 規格）已實作並用真實資料驗證通過，非草案；單層優惠寫法完全不受影響。
- 資料蒐集規格書現行版本為 **v7**（`docs/data-collection/DATA_COLLECTION_SPEC_信用卡優惠資料蒐集與分類規格書-v7-2026-08-04.md`），v6 已加註取代說明；往後指派 Codex 或整理人員任務需明確指定用 v7。

## 驗證與環境

- `tsc --noEmit`：通過（卡面樣板改版後執行）。
- `npm run data:import:dry` / `npm run data:import`：多次執行皆通過，0 錯誤（1 個既有警告，`esun-unicard-wallet-new-card-2026q3` 最低消費欄位歸類問題，非本輪產生，未處理）。
- 純函式單元測試（多層回饋分組邏輯）：通過，未連資料庫。
- 瀏覽器對正式站 `https://credit-card-web-pi.vercel.app` 的多輪核對：卡面新樣式、新增卡片詳情頁、多層回饋優惠詳情頁、商務御璽卡排除邏輯，皆核對顯示正確。
- Production：`https://credit-card-web-pi.vercel.app`，資料庫 Neon PostgreSQL `credit-card-web`（Singapore region），本輪對話結束時為最新資料與最新已部署 commit（`fdc7432`push 後應觸發 Vercel 自動部署，新 session 建議核對 Deployments 分頁確認對應到最新 commit）。
- 本機 dev server：本輪對話有多次啟停，結束時未特別確認是否仍在跑，新 session 需要時自行用 preview 工具重開。

## 下一步

**最優先**：無強制性下一步，以下皆為使用者自行選擇的方向，沒有先後強制順序：
1. T25（優惠網址穩定性與過期轉址）的 5 個待決問題討論並拍板方向。
2. T22（排程輔助資料更新）或 T24（申辦導引連結）草案討論並核准 Scope。
3. 若要繼續挖 2026 下半年新卡，先處理本文件「尚未完成或阻塞」列出的候選（玉山 UBear 卡、聯邦吉鶴卡／賴點卡），需**實際 WebSearch／WebFetch 查證官網**，不可憑經驗編造。
4. 處理本機開發環境直接接正式站資料庫的系統性風險（見上方「尚未完成或阻塞」）。

其他想法見 `ROADMAP_產品路線圖.md`，不在交接中同時開工。
