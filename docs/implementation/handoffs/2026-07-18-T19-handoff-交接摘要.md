# 交接摘要：T19 待驗收（2026-07-18）

依 `HANDOFF_TEMPLATE_新對話交接模板.md` 產生。目前狀態入口仍為 `CURRENT_STATE_目前專案狀態.md`。

## 專案定位

- 正式 Git root：`C:/Users/user/Documents/Credit card web project`
- 工作區 alias：`C:/Users/user/Documents/信用卡查詢網站`（junction，同一份檔案）
- branch：`main`
- HEAD：`43b724f`
- 遠端關係：`main...origin/main [ahead 1]`

新 session 必須重新執行 Git 檢查，不可只相信本文字。

## Worktree 與未提交變更

- `.ai-worktree-lock.json`：本交接文件寫完後由本 session 釋放，新 session 以檔案實際存在與否為準。
- 未提交變更分三批，都不得覆蓋：
  1. 首頁信用卡入口（T14 時期）：`package.json`、`src/app/page.tsx`、`scripts/verify-homepage-card-entry.mjs` 等——使用者已拍板 commit，尚未執行。
  2. T15 治理文件（全部 untracked 的治理與 docs 檔案）。
  3. T19 本輪：`prisma/schema.prisma`、`src/app/cards/[slug]/page.tsx`、`src/app/admin/cards/page.tsx`、`src/components/AdminCardForm.tsx`、`src/lib/admin-actions.ts`、`src/lib/domain-parsing.ts`、`docs/data-collection/`（規格書 v2＋模板 v2）、T19/T20 任務卡、T19 Summary、索引與 CURRENT_STATE 更新。
- 沒有其他已知 AI session 正在寫入。

## 目前任務

- Task：T19 卡片結構化欄位與蒐集規格擴充
- 任務卡：`docs/implementation/tasks/T19-CARD_STRUCTURED_FIELDS_卡片結構化欄位與蒐集規格擴充.md`
- Scope v1 已核准（2026-07-18，使用者原文「核准 T19 v1，排 T16 前，標籤清單照草案」）
- 任務狀態：**待驗收**（實作與自動驗證完成，等使用者人工抽點）
- 部署狀態：不適用
- Git 授權：僅唯讀；add／commit／push 未授權
- 高風險授權：本機 SQLite schema 變更＋備份（已執行完畢）

## 本輪完成（有證據）

1. `Card` 新增六欄（annualFee／annualFeeWaiver／cardLevel／cardNetwork／prosJson／consJson），`prisma db push` 同步成功；備份 `prisma/backups/dev-before-t19-schema-20260718-160058.db`；PRAGMA 驗證欄位齊全、10 筆卡片資料完好。
   - 與任務卡差異：原寫「建立 migration」，因專案無 migrations 目錄（既有流程即 db push）改用 db push，已記錄於 Summary。
2. 前台卡片頁條件式顯示新欄位＋優缺點條列區塊；後台新增／編輯表單與 server actions 支援（優缺點一行一項 textarea ↔ JSON 陣列）。
3. `npm run build` 通過；臨時 dev server（port 3105，已停）冒煙測試：空欄位不顯示空區塊、測試值正確渲染，測試值已還原 NULL 並驗證。
4. 蒐集規格書 v2（`docs/data-collection/DATA_COLLECTION_SPEC_信用卡優惠資料蒐集與分類規格書-v2-2026-07-18.md`）：cards 六新欄、八個消費情境標籤（繳稅、學費、水電瓦斯、保費、加油、外送、超市量販、旅遊訂房）、優缺點可查證原則、情境標籤誠實原則、FAQ Q9。v1 已標註取代。
5. 模板 v2 xlsx（`信用卡優惠資料整理模板-v2-2026-07-18.xlsx`）：cards 插六欄、說明頁更新、DAWHO 資料列保留、無公式。
6. T16 任務卡新增相依備註（匯入腳本以 v2 模板為準、優缺點逐行轉 JSON）。
7. T19 Summary、任務索引、CURRENT_STATE 均已更新。

## 尚未完成或阻塞

- T19 人工驗收（使用者）：後台填新欄位→前台抽點；審閱規格書 v2 與模板 v2。通過後 T19 改「完成」。
- 首頁未提交變更 commit：已拍板但未執行，需使用者當下 Git 操作確認。
- T16 匯入腳本 `scripts/import-offer-data.mjs`：尚未存在（T16 核心交付物），可開工；正式匯入需等整理人員交付 v2 試算表。
- 驗證指令分類（lint／build／smoke）仍 unclassified，T17 前需提分類草案。
- T20 攻略文章功能：任務卡 v1 已起草**待核准**（`tasks/T20-GUIDE_ARTICLES_攻略文章功能.md`），建議 T18 上線後執行。

## 已確認產品決策（仍有效）

- 第一版清除 seed 測試資料；首頁變更採 commit；部署 Vercel Hobby + Neon Free；後台帳號使用者本人持有（皆 2026-07-08）。
- T19 排 T16 前；情境標籤照草案八項（2026-07-18）。
- 使用者核心目標：網站內容能被 AI 引用（情境式查詢如繳稅／學費／水電費）；KOL 內容只參考架構與選題，數字回官方查證改寫，不抄錄。

## 驗證與環境

- 已執行：`prisma db push`（成功）、`npm run build`（通過）、卡片頁 runtime 冒煙（通過）、模板 v2 腳本驗證（通過）。
- 未執行：lint 未單獨跑（build 已含編譯檢查）；git add／commit／push（未授權）。
- 本機 dev server：已全部停止。Preview／Production：未知（未部署）。

## 平行對話補充：CONDITION_SCHEMA 規劃檢視（2026-07-18，dispatch session「Credit Card 專案權限確認」）

使用者同日在另一個對話做了規劃層級檢視，結論如下（該對話未修改任何檔案）：

- 使用者提供了 **`CONDITION_SCHEMA.md` v0.1 草案**（優惠條件 JSON 規格：Promotion／RewardTier／conditions，暫稱可能的「T21」）。**草案原文目前只存在該對話中，尚未存入 repo**；建議落地位置 `docs/superpowers/specs/2026-07-18-CONDITION_SCHEMA_優惠條件結構設計-v0.1-草案.md`（檔頭標示草案待審核，比照 plans 草稿慣例）。
- 規劃檢視結論：
  - Card 側落差已由 T19（規格書 v2）補上；但 **T16 任務卡文字仍寫「以規格書 v1 為準」**，建議做 T16 v1→v2 小幅 Scope 修訂對齊版本引用（T16 的相依備註已提及 v2，Scope 本文未改）。
  - **Offer 側有實質落差**：offers 工作表為扁平結構，多層回饋（如永豐幣倍卡）需靠 FAQ Q3/Q4「寫進文字」的權宜法，與 RewardTier 結構化「資料形狀不同」。建議在規格書發給整理人員前做**輕量 v3 修訂**：多層回饋以固定格式分行填寫＋在附錄記錄已知未來欄位缺口；不需等 CONDITION_SCHEMA 定案。
  - CONDITION_SCHEMA 完整實作應另開任務卡（T21），**不得**掛在 T16／T19 下（兩者 Non-scope 皆排除）。排程兩選項：T18 前（migration 一次做完、上線延後）或 T18 後（快上線、但需在正式 Postgres 做第二次遷移並走 schema 專門流程）。
  - T17、T18、T20 任務卡不需修改；T20 與 CONDITION_SCHEMA 互不依賴。
- **待使用者拍板**：(a) 規格書輕量 v3 修訂要不要做；(b) Promotion 與既有 Offer 的關係（新增 vs 重構）；(c) T21 排 T18 前或後。
- 時效修正：該對話建議「T19 先收尾（模板 xlsx、Summary、CURRENT_STATE）」——這些已於本 session 完成，勿重做；T19 現況以本文件與 CURRENT_STATE 為準。

## 下一步（最優先單一行動）

使用者執行 T19 人工驗收；通過後由執行 session 把 T19 狀態改「完成」，接著處理首頁變更 commit（需 Git 授權）與 T16 匯入腳本。CONDITION_SCHEMA 相關三個待拍板決定（見上節）在 T16 啟動前處理。

## 新 session 必讀

1. `AGENTS.md` → 2. `AI_WORKFLOW_AI協作流程.md` → 3. `CURRENT_STATE_目前專案狀態.md` → 4. T19 任務卡 → 5. T19 Summary → 6. 本交接文件
