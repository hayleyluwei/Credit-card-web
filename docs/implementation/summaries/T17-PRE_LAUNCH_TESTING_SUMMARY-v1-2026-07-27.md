# T17 上線前完整測試 Summary

日期：2026-07-27（Asia/Taipei）
任務卡：`docs/implementation/tasks/T17-PRE_LAUNCH_TESTING_上線前完整測試.md`（v1 已核准，2026-07-27）
任務狀態：自動驗證通過＋AI 示範人工測試完成；**待使用者最終覆核後才可標記「完成」**（依任務卡「人工測試由使用者親自執行，AI 不得代為認證」原則，本次由 AI 示範跑一輪，最終覆核仍需使用者本人）。

## 核准與拍板事項

- 2026-07-27 使用者核准 T17 Scope v1。
- 抽查比例：拍板為「全部 16 筆」（非任務卡預設的 10–20%）。
- 人工測試執行方式：拍板為「AI 先示範跑一輪，使用者最後覆核」。
- Git 授權：拍板為「授權 git add + local commit」（不授權 push）。

## 完成內容

### 1. 正式資料完整性檢查腳本

- 建立 `scripts/verify-release-data.mjs`（唯讀，不寫資料庫）。
- 執行結果：**通過，0 個失敗**。
  - 銀行 6／卡片 10／分類 6／優惠 16（已發布 16）／優惠卡片對應 20，無舊測試資料殘留。
  - 每筆已發布優惠的必要欄位（title/summary/category/對應卡片/sourceUrl/lastVerifiedAt）皆齊全。
  - 2 個警告（不阻擋）：
    1. 2 筆優惠（永豐指定稅款分期、永豐學費回饋）已過期但 `isPublished` 仍為 true——設計上前台依 `endDate` 過濾，AI 示範人工測試已於瀏覽器確認前台正確隱藏，非 bug。
    2. 目前 0 筆優惠標示 `isFeatured=true`，首頁「本期精選」區塊因此為空——**待使用者決定**是否指定精選優惠。

### 2. 既有 smoke 腳本盤點與處置（逐一列出，未默默刪除）

| 腳本 | 處置 |
|---|---|
| `verify-t01-routes.mjs` | 沿用不變（純路由文字檢查，無 seed 資料依賴） |
| `verify-t02-prisma.mjs` | 沿用不變（純 schema 結構檢查） |
| `verify-t03-seed.mjs` | **停用**（已加註頭部說明）：斷言值（3–5家銀行、8–12張卡、≥20筆優惠、admin@example.com、至少一筆草稿/停用/過期）皆為舊 seed 專屬，真實資料不符合，執行必失敗。保留檔案，由 `verify-release-data.mjs` 取代其功能。 |
| `verify-t04-domain.mjs` | 沿用不變（純 domain 邏輯單元測試，用程式內建假資料，非 DB 查詢） |
| `verify-t08-seo.mjs` | 沿用不變，**但目前執行必失敗**：查詢「已發布且 faqJson 非空的優惠」，真實資料 16 筆優惠 faqJson 皆為空（已知缺口，對應 T20/T21），非腳本本身問題，待 faqJson 補齊後才會通過。 |
| `verify-t09-auth.mjs`～`verify-t13-rwd-polish.mjs`、`verify-admin-ux-fixes-v2.mjs`、`verify-admin-ux-fixes-v3.mjs` | 沿用不變：結構上不依賴特定舊 seed 內容（多為自建測試資料的登入/CRUD流程），但需要即時 dev server 與目前正式管理員密碼，且會寫入共用開發資料庫，依驗證政策仍為 `unclassified`，本次 T17 不執行，執行前需使用者核准分類或親自執行。 |
| `verify-real-card-data.mjs` | **停用**（已加註頭部說明）：斷言基於 T16 第一輪局部真實資料（僅10筆優惠）當時的英文卡名與 slug，兩輪匯入後的最終資料集已完全不同，執行必失敗。保留檔案，由 `verify-release-data.mjs` 取代。 |
| `verify-ux-followup.mjs` | **停用**（已加註頭部說明）：斷言引用舊 slug（`cube-cashback-3`、`dawho-high-cashback-2026`）與已不存在的「FlyGo 卡」，執行必失敗。保留檔案，由 `verify-release-data.mjs` 取代。 |

### 3. 人工測試腳本與 AI 示範結果

- 建立 `docs/implementation/manual-test-scripts/T17-上線前測試腳本-v1-2026-07-08.md`，涵蓋任務卡列出的全部 9 項測試範圍。
- AI 用瀏覽器工具對本機 dev server（載入 T16 真實資料）示範跑一輪：首頁、現金回饋分類頁、3 組關鍵字搜尋（永豐／CUBE／Unicard）、優惠詳情頁（來源連結可正確點開）、銀行頁、卡片頁、手機寬度（375×812）、SEO 基本項（title/description/canonical）皆正常，過期優惠正確不顯示於前台。
- **AI 無法示範的項目**（詳見測試腳本文件「待使用者覆核」章節）：後台登入與編輯流程（AI 無管理員登入憑證）、5 個未逐頁示範的分類頁、分頁功能（目前資料量未觸發分頁 UI）。

### 4. 資料抽查清單（全數 16 筆）

- 建立 `docs/implementation/manual-test-scripts/T17-資料抽查清單-v1-2026-07-27.md`，使用者拍板「全部16筆都抽查」（非預設10–20%）。
- AI 用 `WebFetch` 逐一比對 16 筆優惠的官方來源網址與資料庫內容（回饋數字、上限、條件、活動期間）。
- **比對結論**：16 筆來源連結皆可正常開啟（無失效連結），**未發現回饋數字錯誤**。發現以下需使用者留意的項目：
  1. **`cube-tiered-category-rewards-2026`**：自動抓取結果顯示頁面日期與資料庫不符（疑似抓取到舊快照），建議人工開啟官網複核。
  2. **`sinopac-designated-tax-installment-2026h1`、`sinopac-tuition-payment-rebate-2026h1`**：回饋內容與官網相符，但官網目前顯示的活動期間是「2026/7/1–12/31」（下半年），資料庫記錄為已過期的上半年期間。可能是官方將同一活動延展至下半年而資料庫未同步，導致實際可能仍有效的優惠被誤判過期並隱藏於前台。**建議請資料整理人員或使用者確認並視需要更新期間**（T17 Non-scope 不允許 AI 直接修改資料）。
  3. `esun-unicard-wallet-new-card-2026q3` 的 `minSpend` 欄位內容經核對為欄位歸類問題（填入的是申辦期限敘述而非最低消費金額），內容本身無事實錯誤。
  4. HSBC 御璽卡年費 NT$2,500 經官網交叉驗證，與 T16 先前人工手動補上的金額一致。
  5. 另有數筆官網提供但資料庫尚未收錄的追加優惠（HSBC申辦組合加碼、永豐首扣優惠），屬資料蒐集覆蓋率缺口，非既有內容錯誤。

## 未執行（依任務卡 Non-scope 或待使用者執行）

- 未修改產品功能、UI、Prisma schema 或資料內容。
- 未接觸正式環境、未部署、未執行 push。
- lint／build 未於本次獨立執行（驗證指令分類仍為 `unclassified`）。
- 後台登入編輯測試、5 個分類頁逐頁示範、分頁功能驗證——待使用者親自執行。

## 完成定義對照

- 自動驗證通過：✅ `verify-release-data.mjs` 通過，0 錯誤（2 個非阻擋警告）。
- 人工測試腳本：AI 已示範跑一輪並記錄結果，**最終「全部通過」判定待使用者覆核**。
- 資料抽查（全部16筆）：✅ 完成，未發現回饋數字錯誤或連結失效；發現 2 項建議確認的資料時效性問題（見上）與數項覆蓋率缺口，非任務卡定義的「重大錯誤」，但是否需要退回 T16 資料流程修正，**請使用者裁定**。
- 網站是否進入「可部署」狀態：**建議待使用者完成上述「待覆核項目」後再由使用者確認**，AI 不代為做最終判定。

## 下一步

1. 使用者覆核 `T17-上線前測試腳本-v1-2026-07-08.md` 與 `T17-資料抽查清單-v1-2026-07-27.md` 中標示「待使用者覆核」的項目。
2. 視使用者裁定，決定 `sinopac-designated-tax-installment-2026h1`／`sinopac-tuition-payment-rebate-2026h1` 是否需要回到 T16 資料流程更新期間。
3. 使用者覆核通過後，任務狀態可正式改為「完成」，網站進入「可部署」狀態（供 T18 使用）。
